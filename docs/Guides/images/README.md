# Guide screenshots

Referenced by the VM setup guides. Keep filenames stable — the guides link them
directly.

| File | Shows |
|---|---|
| `fusion-settings.png` | VMware Fusion **Settings**, with **USB** under *Removable Devices* |
| `fusion-usb.png` | Fusion's USB device list with **Plug In Action** set to *Connect to Linux* |

Capture at a readable window size, light or dark, and crop to the window. These
ship publicly (`docs/Guides/` is in `.distill`), so make sure no unrelated
device names, hostnames or personal detail are visible in frame.
