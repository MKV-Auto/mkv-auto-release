# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

<!-- Add new entries under the Unreleased heading below. Do NOT hand-write a
     version header or date: the release workflow stamps that heading with the
     computed version and release date at button-press, and inserts a fresh
     empty Unreleased heading above it. -->

## [Unreleased]

## [1.6.5] - 2026-08-23

### Fixed

- **The app fits phone screens again.** On mobile, pages loaded with everything subtly the wrong size — content shrunken with a dead strip down the right edge, while the release dropdown and the bottom action bar drew wider than the screen itself. One frame during page load rendered the disc-card strip at full width before its scrolling container took hold; phone browsers size the page against that single frame and never shrink it back. The page can no longer overflow sideways, the bottom bar stacks into two rows on narrow screens, and phone detection now measures the real screen rather than a value that overflow could distort.

## [1.6.4] - 2026-08-22

### Fixed

- **Choosing a season no longer jumps back to a different one.** With more than one release on a series, picking Season Two could silently reassign the disc to Season One — on a freshly scanned disc as well as when moving one that was already assigned. The disc was linked correctly, then a fallback further along looked up "a release for this show", found whichever came first, and overwrote the choice. That fallback was safe while a show could only have one release; allowing several in 1.6.3 is what made it wrong. It now honours the release you picked, and where it genuinely cannot tell which one is meant it leaves the disc alone instead of guessing. Reported and root-caused by @Medformatik (#9).

## [1.6.3] - 2026-08-22

### Fixed

- **A show can now have more than one release.** Creating a second release for a series — Season Two alongside Season Three, say — did not work, and quietly did damage. No new release appeared, and the details you typed for the new one were written over the existing one, so correcting Season Three and then adding Season Two left you with a single entry holding a mixture of both. Behind this, a show was only ever allowed one release, because every season of a series is filed under the same show. Seasons and editions can now sit side by side, each keeping its own year, UPC, ASIN and artwork, and a disc can be assigned to whichever one it belongs to. Adding a new release no longer alters an existing one.
- **The create form no longer forgets what you typed.** Filling in a new release or boxset, clicking somewhere else, and coming back lost every field and dropped you back on the list. Your entry is now still there when you return. Cancelling still clears it, and if a create is rejected the form keeps your values so you can fix the problem without typing it all again.

## [1.6.2] - 2026-08-15

### Fixed

- **The Ungroup button works again.** When titles were wrongly detected as duplicates — several episodes of the same length collapsed into one row, say — pressing Ungroup did nothing at all: no change, no error, nothing. Three separate things were wrong. The page was never told which disc the titles belonged to, so the request was never sent. Once it was sent, the grouping was recalculated as though you had never pressed the button, so the title stayed hidden. And saving a label afterwards put the title back in the group anyway. Ungroup now really does split a title onto its own row, the list updates as soon as you click, and Re-group puts it back. "Make primary" was unreachable in the same way and also works again, including in the mobile drawer. If something does go wrong you get a message instead of a button that seems dead.

## [1.6.1] - 2026-08-12

### Fixed

- **Editing a release in the Library no longer gets undone by labelling a disc.** If you corrected a release's UPC, artwork or year and then labelled a disc that resolved to that same release, your changes were silently reverted — the labelling form still held the values from before your edit, and it wrote them back over the top. Nothing reported a problem, so the only way to notice was to go back and look. Labelling now fills in details a release is missing and never overwrites ones already set.

## [1.6.0] - 2026-08-09

### Added

- **Extras can now belong to the whole show, one season, or a single episode.** Behind-the-scenes clips, featurettes, trailers and the rest used to be filed against the whole show, even when they plainly belonged to one season. Series extras now carry a "Belongs to" control: when everything on a disc is one season the answer is filled in for you, a boxset bonus disc asks per extra, and a deleted scene can be pinned to its exact episode. On Plex an episode-level extra attaches to the episode itself; Jellyfin has no episode extras, so it files by season — but the episode you chose is still recorded in full for TheDiscDB, so what you contribute is the same on either server. The filename preview under the form shows exactly where each choice lands the file. See [Extras](docs/Guides/EXTRAS.md) for the layout rules and Plex agent notes.

### Fixed

- **A disc waiting to be labelled no longer blocks the next rip.** The check for "is a rip already running on this drive" counted any job that hadn't been closed out — and a job stays open through labelling and transfer. On a drive with finished discs still waiting on you, starting the next rip could be refused, and in some paths the waiting job was written off as stale and marked failed.

- **An empty drive no longer shows a disc as "Now Reading".** A disc you finished with days ago could reappear in the carousel as though it were sitting in the drive, then vanish the moment you inserted a real one. The check for "a rip is in progress" only looked at whether the job was closed out, and a job stays open through labelling and transfer — so every disc that had ever passed through that drive still claimed it.

- **The support note in the notifications panel no longer flashes and disappears.** It was hidden whenever a job was still open, but a job stays open through labelling and transfer — long after the disc has finished ripping — so on a machine you actually use, the note appeared for a moment on page load and then vanished. It now stays put until you answer it.

## [1.5.0] - 2026-08-08

### Added

- **A "Download support bundle" button under Settings → Help.** If a disc or drive isn't behaving, this collects the logs and everything about how your drives are wired into one file you can attach to a bug report — instead of us asking you to run a series of commands and paste the output back. Passwords and API keys are stripped before the file is written, and it's a plain `.tar.gz` you can open and check first. It also prints what it found: most drive problems have a specific cause, and the bundle names it. The button is unavailable while a rip is running, because collecting drive information would interrupt the rip — come back once it finishes. Reading how Docker itself is set up isn't possible from inside the container, so for those cases there's a script you can run on the host, and the page tells you how to get it.

- **A "Buy me a beer" link in the header.** MKV-Auto stays free and always will be — this is just a way to chip in if it's saved you some time. It sits with the existing Website / GitHub / TheDiscDB links, and in the menu on mobile.

- **A one-off support note in the notifications panel, after five successful rips.** It only ever appears once the app has actually got five discs through for you, never while a job is running, and it waits behind the bell rather than interrupting. "Don't show again" means exactly that, and three "maybe later"s are taken as a no. The choice is stored on the server, so dismissing it on your desktop also dismisses it on your phone.

### Fixed

- **A multi-part episode no longer blocks you from finishing a disc.** When a disc splits one episode across two playlists and you labelled both as the same episode with Part 1 and Part 2, finishing the disc failed and claimed a title wasn't labelled properly — even though the list showed everything filled in. The check that guards against two titles producing the same filename wasn't looking at the part number, so it treated the two halves as the same file. It now accounts for both the part number and the episode range.

- **The "not properly labeled" error now tells you which problem you actually have.** Two different failures shared one message: a title genuinely missing a field, and two titles that would write to the same filename. The second now says so and suggests what to change, instead of sending you looking for a blank box that isn't there.

## [1.4.0] - 2026-08-07

### Fixed

- **The Layout setting no longer reverts to "Single episode" after you set it.** Choosing "Split across files" saved correctly but snapped back on the next refresh, because the layout values weren't included in the data the page reloads.

- **The TMDB episode dropdown comes back on discs you've already linked.** On a disc labelled in an earlier session the episode picker was silently missing — the app looked for the TMDB link in one place and the disc had it in another, so no episode list was ever fetched. It now follows the disc's own link to the series, and fetches the season the title actually belongs to rather than only the disc's first season. The picker still appears only on titles typed as Episode.

- **Clips you label inside a "play all" extra now appear under it in the title list.** They were kept and ripped, but stayed hidden behind the combined playlist so there was no way to see what you'd labelled. Labelled clips now sit indented beneath their playlist; unlabelled ones stay collapsed as before.

- **Labelling a clip inside a "play all" extra now actually rips it.** Some discs bundle their featurettes into a single play-all playlist. You could already label the individual clips inside it, but they stayed hidden and only the long combined file was produced. A clip you label is now kept and ripped on its own, and the combined play-all steps aside so the same footage isn't written twice — set a type on the play-all itself if you want both.

- **On a series disc you can now name extras.** The labeling form used to decide which fields to show from the disc as a whole, so every title on a series disc got the episode dropdown and no name box — a featurette or trailer simply could not be named. Fields now follow each title's own type: pick Episode and you get the episode picker, pick anything else and you get a name field. The name, season and episode boxes also stay available on episodes now instead of disappearing once TMDB data loads, so you can correct entries where the disc and TMDB disagree.

- **Episodes that don't line up one-to-one with files can now be labelled.** A new Layout setting on episode titles handles both cases discs actually present: an episode split across two files (written as `part1`/`part2`, which Plex and Jellyfin play as a single episode), and one file covering several episodes (written as `s03e01-e02`). The filename preview updates as you choose, so you can see what you'll get. Where TMDB numbers a two-parter as separate episodes with "(1)" and "(2)" in the name, that's now recognised automatically and the marker is dropped from the filename — anything you set by hand still wins.

- **A disc that splits one episode across two files no longer loses the second one.** When two titles were labelled with the same season and episode, they produced the same filename — and post-processing treated the second one as "already done", left it behind, and still reported the job as successful. It now stops with a message naming both titles so you can give them distinct episode numbers. (Star Wars Rebels season 3 does this: the premiere "Steps Into Shadow" is two files on disc but one entry in TMDB.)

- **A transfer can no longer run twice at once.** The app had two separate ways of starting a file transfer and neither checked whether one was already running, so the same job could be sent twice and two copies would write to the same destination file at the same time. On an SMB share this showed up as write timeouts part-way through a batch. Only one transfer per job can now be started, whichever path asks for it.

- **The notifications panel no longer opens off the edge of the screen.** On desktop the bell sat next to the app name in the top-left, so its right-aligned panel opened leftward and was clipped by the window — about 80px lost at 1600px wide, and 160px at 1280px. The bell now sits at the far right of the header with the other icons, and the panel is capped at the window width so it cannot overflow on narrow phones either. The panel is also no longer see-through — page content used to read through it.

- **A drive MakeMKV can't see now tells you why, instead of "Disc not found".** MakeMKV finds optical drives through a different device node than the rest of the system (`/dev/sg*`), so a drive that works everywhere else — it shows up, it mounts, you can read from it — can still be invisible to the ripper. That showed up as a bare "Disc not found", pointing you at the disc when the disc was never the problem. The app now recognises the case and tells you which of the two causes you have: the host is missing the SCSI generic kernel module, or the module is loaded and those nodes never reached the container. Each gets its own instructions, including that loading the module needs a container restart to take effect and needs a config file to survive a reboot. Full walkthrough in `docs/HOST_OPTICAL_SETUP.md`.

- **A slow download can no longer be mistaken for a broken FFmpeg version.** The daily compatibility check treats a version pair as untested when it cannot fetch the sources, rather than recording it as a build failure. Previously one timed-out download was remembered as a permanent verdict and the pair was never retried — which would have held the app on an older FFmpeg indefinitely. Genuine build failures are still recorded.

## [1.3.0] - 2026-08-06

### Changed

- **Your hand-typed labels now survive every automated pass.** Every label field (name, type, season, episode, edition, description) now remembers whether a human or an automated step set it, and automation can only ever update its own answer — never yours. Concretely: re-running a DiscDB lookup, rescanning a disc, or the duplicate-title housekeeping can no longer replace a name you typed. Where the app previously defended your edits with timing heuristics, it now simply cannot lose them. Existing labels are migrated in place (with the usual automatic pre-migration backup); rows you had labeled by hand keep your ownership.

- **Editing a title name no longer touches any other row.** Renaming or describing a title used to re-run duplicate-group housekeeping across the whole disc on every save — hundreds of hidden row updates and a large response payload per edit. That housekeeping now runs only when something that actually affects grouping changes (a type change, a rescan, saving the label form). Text edits are a single row write.

- **Opening a disc no longer modifies it.** Loading the titles view used to write duplicate-detection marks to the database as a side effect of reading it — which could hand the page state that was already out of date the moment it arrived. Those marks are now computed when the disc is scanned or re-scanned, and reads are just reads.

- **A second window now sees your edits immediately.** Title edits are broadcast as small change notices carrying just the rows that changed, and other open tabs fold them in on the spot. Previously a second tab either refetched the entire disc (about 1 MB on a 300-title disc) or never found out at all.

### Fixed

- **Typing a title no longer saves on every keystroke, and no longer loses what you typed.** Each character you typed sent its own save, and each reply wrote the server's copy back into the box you were still typing in — so the field would visibly reset and re-type what you had just entered, sometimes dropping the last character or two. Fields you type into (title, edition, description, season, episode) now save once, shortly after you stop typing — and immediately if you click away first. Several edits to the same title are sent together. The type dropdown still saves the moment you pick it, and a name you typed just before rides along in that same save so the two can't race each other.

### Fixed

- **Changing the type right after typing a name no longer reverts the name.** Two things conspired here. First, picking a type while a just-typed name was still waiting to save sent two saves at the same instant, and the server's version check wasn't atomic — both claimed the same version number, after which no later refresh could tell the states apart; the row is now locked during the check so that can't happen. Second, when a save was rejected as stale — routine, because background housekeeping bumps title versions constantly — the app silently threw your text away and put the server's copy back in the field. You'd type a name, reach for the type dropdown, and watch the name snap back; the type would then save fine, which made the type change look like the culprit. A rejected save now simply retries once against the version the server reports, so your input wins over background churn; only a genuine tug-of-war with another editor shows the conflict message. You never have to wait for the autosave pause — moving straight to the next field, row, or dropdown carries your edit with it.

- **Editing titles on the phone-sized layout no longer fights your typing.** The mobile title editor was still saving on every keystroke — it had missed the earlier fix — with all the same echo-and-revert symptoms. It now buffers and saves exactly like the desktop editors.

### Fixed

- **Edit conflicts stopped being invented, and stopped costing you the form.** The app used to guess what the next version of a title would be; any change it hadn't seen made the guess wrong, and a wrong guess was reported to you as a conflict with someone else's edit. It now simply tells the server which version it read and lets the server decide, so those phantom conflicts disappear. When a genuine conflict does happen, the server sends back the winning value and only that one row updates — previously the whole title list was refetched and replaced, so one conflicted row could clear everything else you had typed.

### Fixed

- **A background refresh can no longer undo what you just typed (#778).** While you label, the app periodically refetches the disc's state in the background. That refetch is a snapshot of the moment it was requested, but it was applied whenever it arrived — so an edit made in between was silently replaced by the older value, with no error shown. Renaming a title and then changing its type was a reliable way to trigger it. Refetched titles are now merged row by row, keeping whichever version is newer, and text you are still typing is never overwritten at all.

### Fixed

- **Labeling no longer erases your in-progress edits (#775).** Editing a title — especially changing its type — could wipe everything else you had typed. Behind the scenes, every edit re-ran the duplicate-group sync, which re-modified already-demoted rows on every pass and silently bumped their edit counters; your next edit then looked like a conflict, and the conflict recovery replaced the whole table with server state. The sync is now idempotent, patch responses tell the page exactly which rows the sync touched, and conflict recovery replaces only the row that actually conflicted. A type you set by hand on a duplicate row is also respected now instead of being re-fought on every edit.

### Added

- **A disc the drive can't read now tells you to reseat it.** Inserting a disc upside down or not quite seated used to do nothing at all: the app saw no readable media, treated it as an ejection, and said nothing, so the only symptom was that the disc never scanned. That case is now told apart from an empty drive — the drive senses something in the tray but cannot read it — and you get a notification asking you to eject and reinsert, with a note that a dirty or damaged disc looks the same. An empty drive stays silent, and a disc left misseated alerts once rather than on every retry.

### Fixed

- **Installing MakeMKV works again.** As of 3 August, installing or updating MakeMKV failed partway through with a wall of compiler errors (`'AVCodec' has no member named 'ch_layouts'`), which blocked first-time setup completely — nothing on this end changed, FFmpeg simply published version 9.0 that day and the installer always grabbed the newest release. FFmpeg 9 removed pieces that MakeMKV still relies on, so the installer now builds against the newest FFmpeg that MakeMKV actually supports. Ordinary FFmpeg updates still come through automatically; only the incompatible jump is held back, and the log says so plainly when it skips one. Existing MakeMKV installations were never affected, and a failed install never damaged a working one.

- **The "advanced FFmpeg features" checkbox in Settings now does something.** It was sending the wrong option, so unticking it changed nothing — you got the non-free AAC codec either way. It is now wired to the setting it names, and the label matches the equivalent one in first-time setup.

- **Installing MakeMKV now works even when makemkv.com is unreachable.** The installer has always had an archive.org fallback, but it only ever looked at a single archived copy, and it reported *any* problem reaching the archive — including being temporarily rate-limited, which happens routinely — as "this file isn't archived". Installs failed with a message saying the file was unavailable when it was sitting in the archive the whole time. It now consults the full archive index, skips broken or partial copies (some archived entries are error pages saved during an earlier outage), and works through the remaining copies until one checks out. Being rate-limited is now retried and, if it persists, reported as the temporary condition it is rather than a missing file. Verified against the real outage: the download completed and matched the official file exactly.

### Added

- **MakeMKV updates are now offered only in combinations we've tested.** A background job builds each new MakeMKV release against the FFmpeg versions it might use, and only combinations that compile and produce a working program are published. Your installation checks that list — so an update that would fail to build is held back rather than offered, and when one is held back the app can tell you why instead of silently showing nothing. Downloads are also checked against the fingerprint of the exact files that passed those tests, which matters most when a file comes from the archive rather than from MakeMKV directly. Until the first list is published, installs behave exactly as before.

## [1.2.1] - 2026-08-01

### Fixed

- **An empty drive no longer reports a disc as loaded (#766).** The app decided whether a disc was present by asking the drive for its size, and USB Blu-ray drives report a phantom size with an empty tray — so an empty drive looked loaded, which affects the "Insert Disc" prompt and the check guarding the start of a copy. It now asks the drive directly whether media is present, using the tool the container was previously missing and falling back to the kernel's own answer rather than to size. If neither can answer, the drive is still treated as usable so it never disappears from the interface, but that is now recorded in the log instead of passing silently. Verified against the drive that showed the fault.

## [1.2.0] - 2026-07-30

### Added

- **The library page is where you contribute to TheDiscDB now (#741).** A strip under the tabs tells you how many discs aren't in TheDiscDB yet, with one **Export all** button; a **Contribute** tab filters the library to just those entries; and every release, boxset, and disc shows its state at a glance — amber for "ready to export", teal for "already in TheDiscDB". The strip's count comes from the same rule the export runs, so what it says is exactly what you get. Exports run in the background with progress and land as a download when done.
- **Corrections flow back to TheDiscDB (#741).** If a disc matched TheDiscDB but you've since fixed its titles or details — because upstream had it wrong — it's marked "changed since TheDiscDB" and joins the export as an **update**: the zip targets TheDiscDB's own directory for that disc, so in your fork the change shows up as modified files, exactly what an update should look like in a pull request. An update ships only the disc files — never the release metadata or cover art, which belong to the existing entry — and it starts from TheDiscDB's committed copy and lays your data over it, so hand-written chapter names, per-title descriptions, and the disc's `GlobalDiscId` survive instead of being deleted by your diff. The files are even written in upstream's own serialization style, so unchanged lines stay unchanged: validated live against a real disc, where the pull-request diff came out as exactly the one corrected field. The zip's README separates new entries from updates, names the exact files each update replaces, says what `git status` will show, and warns that macOS Finder's drag-and-drop replaces folder contents instead of merging. When an export containing updates finishes, the library page says so in a dialog — and both it and the README hand you a **suggested commit message** computed against upstream's current entry: who produced the update, which files it replaces, and every correction with the prior value it replaces, stated expressly. One click copies it, ready to paste into your commit. Only genuine corrections count: edits to the surfaces a human fixes — title names, types, seasons, episodes, descriptions, the disc's name or format — mark a disc as changed, while filenames, technical values, renumbering, and re-saving the same value never do, and an update keeps upstream's own filenames. If what remains matches TheDiscDB exactly, the export ships nothing and says why. Every disc can also be re-exported by hand from its details drawer, for staleness the app can't detect.
- **Exports now match TheDiscDB's repository completely (#753, #754, #755).** Boxsets are written the way upstream models them — a small `boxset.json` index under `data/sets/` pointing at the member films, with the disc files under each film's own folder — and brand-new films include the film-level `metadata.json` and cover art that every upstream entry carries. Internal pipeline fields no longer leak into the files, and track details now come from the scan's stored MakeMKV log even after job artifacts are cleaned up, so every title ships with its video and audio streams filled in. Verified by unzipping a real export over a clone of TheDiscDB's repository and checking what git saw.
- **Tidier library cards.** The always-visible Edit and Delete buttons on every card are gone — each card has one quiet ⋯ menu with Edit, a count-aware Export, and Delete behind a divider. Finalized releases keep their menu items visible with a 🔒 and the reason, instead of graying out silently.

### Fixed

- **The migration safety net now fails closed in every branch (#757).** Found during an upgrade rehearsal against real data: if the startup script couldn't *read* the database's current revision (a permissions or timing problem), it treated the database as brand new — skipping the pre-migration backup and attempting to migrate anyway — and a failed migration exited as if it had succeeded, so only the API's refuse-to-serve gate stood between you and a half-migrated database. Now an unreadable revision means no migration at all (with the reason recorded), a failed migration reports failure, and `db-restore.sh` restores tables owned by the app account rather than locking the app out of its own database.

## [1.1.0] - 2026-07-29

### Added

- **Set up the container from Docker environment variables (#743).** Your MakeMKV and TMDB keys, media server, Discord webhook, auto-rip, eject behaviour, path templates and preview settings can now be supplied as `MKVAUTO_*` variables, so a fresh container comes up already configured with no setup wizard to click through — useful for Compose stacks and Unraid templates. The environment is re-applied on every start, not just the first, so editing your Compose file and restarting actually changes the setting. Anything you pin this way shows as read-only in the web UI, with a note saying where the value came from, because a change made there would be reverted on the next restart. Full list in the [Configuration guide](docs/Guides/CONFIGURATION.md#unattended-setup-application-settings), and they're all in the Unraid template too, so you can set them from the template UI rather than hand-editing extra parameters.
- **The DiscDB export is now a complete submission (#741).** "Export DiscDB bundle" used to hand you a single JSON file that you then had to split into the three files TheDiscDB wants, find the MakeMKV log for, censor yourself, and file in a directory you worked out by hand. It now gives you a zip laid out exactly like TheDiscDB's repository — unzip it into your fork, commit, open a pull request. The MakeMKV log is included and **censored**: your drive's model, serial number and device path are replaced with `***`, matching what TheDiscDB's own entries look like. Cover art comes along too — front always, rear when you've added one — and anything the export couldn't fill in for you is listed in a README inside the zip, so you can see what's missing before you submit rather than after. Discs are also given TheDiscDB's `GlobalDiscId` now — it only exists on the physical disc, so discs you ripped before this release won't have one, but simply putting the disc back in the drive fills it in; no backfill step, and nothing already stored is overwritten. The Export button is disabled with an explanation on discs that aren't labelled yet, instead of failing after you click it. And **Settings → Export & Import** has an **Export DiscDB submissions** button that packages your whole library in one go — unzip it over your fork and open a single pull request, with anything skipped listed in the zip's README so you can see what didn't make it. It runs in the background with a progress count and a cancel button, so a big library doesn't tie up the page. You can navigate away or reload — if it finished while you were gone, the page offers a **Download last export** button rather than making you build the whole thing again (archives stay available for six hours).
- **Runs on ARM now — Raspberry Pi 5, ARM NAS boxes, and Apple Silicon (#732).** The image is published for arm64 as well as x86_64, and Docker picks the right one automatically, so `docker pull` is unchanged. Verified the whole way through on ARM: disc scan, MakeMKV, and a full Blu-ray rip producing a playable file.
- **Windows and macOS setup guides ([here](docs/Guides/VM_SETUP.md)).** Docker Desktop on Windows and Mac can't hand your optical drive to a container — that's a limitation of Docker on those platforms, not something a setting fixes. The workaround is to run a small Linux VM and pass the drive to it. The macOS guide is tested end to end; the Windows one follows the same steps but hasn't been fully verified yet, so tell us how you get on.

### Fixed

- **Drive alerts no longer repeat, and return when the problem does (#723, #724).** Alerts that aren't tied to a copy job — a drive that stops responding, a drive swapped at the OS level, a copy blocked by an unhealthy drive — were meant to show once per problem, but every re-detection sent another one, filling the in-app bell and your Discord channel with the same message. They now show once. And if you fix the drive and it fails again later, you're told again instead of being silently suppressed for the rest of the day.
- **A drive that stops responding no longer shows the wrong movie (#723).** When a drive stopped answering, the app kept going and served the *last* disc it had read — so the card showed a movie that wasn't in the tray, and starting a copy would have filed it under that wrong title. The app now stops as soon as the drive fails to answer, clears what it remembered about that drive, and shows the drive's error instead of a disc.
- **Unreadable drives now tell you what's wrong (#724).** The drive card shows the problem and what to do about it — "Drive is not responding. Try power cycling the drive." — and you get an alert (in-app, plus Discord if you've set it up) the moment it's detected, instead of discovering it hours later. Start Copy refuses on that drive with the same message rather than failing partway through.
- **Discs with no readable titles show their name instead of "Drive 0" (#723).** When a scan comes back empty, the app now falls back to the disc's own label — so you see something like "Star Wars Rebels S3 D1" and can tell which disc it is.

## [1.0.4] - 2026-07-25

### Added

- **The running version is shown in the header (#718).** A small version tag (e.g. `v1.0.4`) now sits under the MKV Auto wordmark on every page, so you can tell at a glance what you're running — and it pairs with the update banner when a newer version is available.

### Fixed

- **A disc that fails its first scan no longer gets stuck unreadable (#720).** If a disc's very first scan didn't finish, the app remembered the disc but never retried the scan — so it appeared in the UI, let you assign a movie, then refused to copy with "no tracks were enumerated", and ejecting and reinserting changed nothing. The app now retries the scan for any disc it hasn't successfully scanned, and a successful scan clears the earlier failure.
- **Copy errors explain the real problem (#720).** When a disc can't be copied, the message now says what actually went wrong — for example that MakeMKV couldn't read or decrypt the disc — instead of always telling you to eject and reinsert, advice that can't help in most of those cases.
- **Issues linked to a public bug report close automatically again on release (#717).** The release automation depended on a third-party action that was deleted from GitHub, so it failed on every release; it's now built in.

## [1.0.3] - 2026-07-21

### Added

- **The running version is shown in the header (#718).** A small version tag (e.g. `v1.0.3`) now sits under the MKV Auto wordmark on every page, so you can tell at a glance what you're running — and it pairs with the update banner when a newer version is available.
- **Upgrades can't lose your library (#709).** Before applying any database change on startup, the container now takes an automatic backup and refuses to serve if a migration fails partway — so an update can never leave you with a half-changed, broken database. Backups are kept under `/data/backups` and can be restored with a bundled script.

### Fixed

- **Adding a disc to an existing boxset no longer stalls it (#711).** Choosing "add to existing boxset" could create the disc's release without a name, which silently blocked that disc from finishing labeling (no error, no progress). The release now always takes its name from the boxset, so the disc flows through to transfer like the rest of the set.
- **Large 4K transfers no longer time out (#712).** Sending a big UHD title (50–90 GB) to an SMB share could fail with "Transfer timeout" and retry forever — the copy had a fixed one-hour limit a large movie can exceed, especially over a busy connection. The limit now scales with file size, so large transfers run to completion instead of being cut off mid-copy.

## [1.0.2] - 2026-07-21

### Added

- **The app now tells you when an update ships (#699).** A slim, dismissible banner appears under the header on every page when a newer release is published — with a link to the release notes. Dismissing it silences that version only; the banner returns for the next release. Privacy-conscious by design: the check runs only while you're actively using the UI (the backend never phones home on its own), it's answered from a 6-hour cache, and dev builds never check at all.
- **Faster title labeling (#701).** Each title in the left rail now has a quick-ignore ✕ so you can drop a title without opening its editor. On mobile, title cards gain a play button that opens the title's preview clip inline (dimmed when no preview exists yet). The preview popup also lost its redundant second close button — the ✕ is all you need.

### Fixed

- **Labeling fields no longer eat your typing (#695).** Editing the disc name or slug could silently drop the last character — or a whole word — after clicking out of a field. Two causes: every keystroke fired its own save whose response was applied back over the form (so a slow response reverted anything typed in the meantime, and responses landing out of order made it feel random), and the name field's keystrokes could land in a stale copy of the form under change-detection churn. Saves are now debounced with strictly latest-wins ordering, save responses can never overwrite text you've typed since, and name edits actually persist on blur (they previously only saved as a side effect of other actions).
- **Switching job cards no longer drags the previous card's step along (#693).** Clicking from a job on, say, the transfer step to one that should be on labeling could land the new card on transfer — with its action bar to match and no way back short of a page refresh. The previous card's state was being saved into the wrong slot of the context cache during the switch, and the new card then restored it as its own. Each card's state is now keyed strictly by the card it belongs to, and a card being viewed can never inherit another card's step.

## [1.0.1] - 2026-07-20

### Fixed

- **Beta keys register again (#688).** MakeMKV's `reg` command rejects valid beta (`T-…`) keys that MakeMKV itself accepts, so entering the current forum beta key failed with "Invalid registration key" — and when MakeMKV reported registration as required, that hard-blocked first-run setup entirely. Keys are now validated by planting the candidate in an isolated sandbox and reading makemkvcon's own startup verdict (its `MSG:5020` invalid-key message is definitive, even mid-trial), then committed to the real settings only on a clean pass — an invalid stored key actually *degrades* a working trial, so the sandbox isn't just tidiness. Purchased (`M-…`) keys were never affected. The wizard now also relays the specific failure (invalid key vs "update MakeMKV" vs malformed) instead of blaming typos, and says plainly that the key is optional while MakeMKV's trial is active.
- **The setup wizard's Plex/Jellyfin choice actually saves now (#689).** Selecting Jellyfin during setup only updated wizard memory; the backend stayed on Plex and postprocess used Plex naming. The choice persists the moment you make it.
- **Creating a release no longer eats your input on failure (#685).** The create form closed itself before the server answered, so a rejected create (duplicate, bad UPC, network) threw away everything you typed. It now stays open with your values and shows the error inline, closing only on success. The release picker also no longer collapses when you switch browser tabs and come back.


## [1.0.0] - 2026-07-18

### TL;DR — first public release

After ~5 months of in-the-wild iteration on the `0.9.0` internal milestone, MKV-Auto's first public release. Highlights:

- **Library redesign.** Compact disc-drawer title cards with type-coloured edges + display/edit mode (#601), auto-ignored junk hidden by default with a one-click "show ignored" toggle (#599), and an "Already in Library" banner on re-insert so you know before you re-rip (#603).
- **Multi-drive concurrent ripping (beta).** Stable-identity by-id tracking, per-disc workflow contexts, drive-swap notifications. Known caveats listed under "Known limitations" in the [release notes](docs/RELEASE_NOTES_v1.0.0.md).
- **Pipeline collapse.** Single CTA on the transfer step now runs post-process + transfer in one user action with a single completion notification (#365, #605).
- **TheDiscDB v2.** Disc-hash lookups against TheDiscDB drive identification and label prefill — read-only, nothing is uploaded. Search results show an "In Library" chip on titles you already own (#590). A guided contribution flow is planned; labels persist locally, so existing rips can be contributed retroactively when it ships.
- **Settings polish.** Notifications surface ordered Errors → Action Required → Informative with the matrix tucked behind the master toggle (#609); TMDB key visible with parity to MakeMKV (#610); Previews input styling fixed (#608); Notifications + Previews + MakeMKV cleaned up.
- **Transfer path tracking.** SMB / rsync / NFS destinations now register correctly in the Library disc panel (#607). Plus a one-shot back-fill script for installs that already have stale rows.
- **Robust stage-state delivery.** Verifying spinner + transfer view advance without a page refresh — `rip_state` / `post_state` / `transfer_state` ride on every progress emit (#604, #605).
- **Lots of small wins.** Library page load 9.2s → 0.8s, disc drawer 30s → 0.4s on large discs (#530, #600); duplicate-group consensus fix; rip carousel polish; transfer history human-readable identity (#593); Discord notification preference matrix; ui-checkbox primitive (#595); and a long tail of UX and reliability fixes you can read about below.

The full diff is large. Treat the bulleted entries below as a chronological log; the TL;DR above is what to skim if you're upgrading.

### Added

- **Setup Assistant now shows the real MakeMKV EULA (#625).** The container downloads MakeMKV source tarballs on first boot and extracts the End User License Agreement text; the Setup Assistant links to it before you click Install, so consent gates the *actual* license instead of a promise. Downloads are cached in `${MKVAUTO_TMP_DIR}/makemkv-download/` and persist across container restarts. The Install button reuses the pre-fetched sources — no network work at install time — and falls back to inline download if the pre-download failed.
- **Destinations are now probed for what they actually let us do (#635 commit B).** When you activate a TransferConfig, MKV-Auto writes a small `.mkvauto-probe-*` file at the destination and tests four primitive ops — new-write, in-place overwrite, delete, rename — so the transfer path knows the share's real permissions instead of guessing. A new strategy selector maps the `Overwrite / Skip / Fail / Rename` intent onto whichever primitive combination the destination allows: a write-once SMB share with delete permission now executes "overwrite" as delete-then-copy proactively, without waiting for the reactive fallback from commit A to catch the `NT_STATUS_ACCESS_DENIED`. Results cache on `TransferConfig.config_data['capabilities']` and push to the UI via a `transfer_config_capabilities_updated` websocket event; a new `POST /system/transfer/configs/{id}/probe-capabilities` endpoint lets the frontend re-probe on demand.
- **Transfer configs surface what the destination lets us do (#635 commit C).** Each TransferConfig card in Settings → Transfer now shows a capability pill next to the health pill: `Overwrite: direct` (green), `Overwrite: delete+copy` (cyan) for write-once shares, `Overwrite: via rename` (amber), or `Overwrite: unavailable` (red) when the current `conflict_resolution` can't be satisfied. Configs that were created before the probe module shipped (or never activated) show a "Probe destination" button so you can trigger the probe manually. Closes the frontend piece of #635; combined with commits A + B, the transfer path now handles write-once shares gracefully both proactively (probe → strategy selector) and reactively (delete+retry on `NT_STATUS_ACCESS_DENIED`).

### Changed

- **Release distillation now strips dev-only code.** `scripts/distill.sh` runs `strip_devmode.py` over the distilled Backend and drops the strip tool + its marker test, so the public source tree and the GHCR image contain no devmode code paths (the `ENABLE_DEVMODE` gate remains but has nothing to enable). Releases land in `mkv-auto-release` as one squash commit per version; the first versioned release replaces the pre-release coming-soon history.
- **TransferConfig Advanced Options is simpler (#629).** Three redundant toggles were dropped: **Cleanup Source After Transfer** (post-transfer cleanup is now unconditional — the periodic reconciler was already doing this within an hour), **Enable Deduplication** (folded into Conflict Resolution: the `skip` strategy now runs the hash pre-flight itself; the other strategies don't need one), and **Enable Notifications** (Settings → Notifications is the source of truth for delivery filtering). The Conflict Resolution default is now **Overwrite** (matching the DB default and typical re-rip intent).

### Fixed

- **Fresh-install MakeMKV install now uses the pre-downloaded version (#669).** The Install button was documented to reuse the sources pre-fetched on boot (#625), but the install path actually re-resolved the version independently and re-scraped — a non-deterministic lookup that falls back to a stale Wayback snapshot could install a *different, older* beta than the one pre-downloaded. A stale beta can't rip out of the box. The install now defaults to the pre-downloaded "ready" version before any scrape, so a fresh install gets the same current beta the Setup Assistant showed the EULA for. Version-agnostic — no version is baked into the image, and container upgrades still keep the existing `/data` install.
- **Release selector now shows resolution so you can tell releases apart (#672).** When linking a disc to a release, the picker showed only name + year + slug — so a 1080p Blu-ray whose disc hash matches TheDiscDB's **4K (2160p)** release entry looked nearly identical to the right one. The dropdown options and the selected-release header now include the release resolution (e.g. "Predator 2 · 2160p · 2018"), making the format obvious at a glance.
- **Disc-swap race no longer wastes a 20-minute rip (#638).** When a udev-triggered scan of a new disc races with the previous disc's cleanup, MakeMKV could return an empty tracklist and the disc row was persisted with `format=NULL` and zero titles — `scan_state` still ended up `completed`, `Start Copy` stayed enabled, and the rip only failed at post-rip verification with "no MKV outputs found". Two guards close the window: the persist path now marks the disc `scan_state='failed'` with a clear "eject and reinsert" hint when a scan returns no format and no tracks; `POST /jobs/rip` refuses dispatch with `409 disc_scan_incomplete` when the target disc has zero `disc_titles` rows.
- **Ignore button now reads as an action, not a state (#642).** In the title editor and duplicate-drawer, the Ignore button used to say "Ignored" (past tense) once a title was ignored — a click would silently un-ignore, which was confusing when batch-clicking through many titles on a Blu-Ray with lots of extras. The button now reads "Un-ignore" when the title is already ignored so the click semantics are explicit. Fixes the toggle-semantics part of the label-UI issues surfaced during the smoke matrix.
- **Blu-Ray m2ts clips inherit the wrapping mpls's ignore decision (#642 sub-3).** When the user labels a `.mpls` playlist (e.g. `00101.mpls` = the main movie), the individual `.m2ts` component clips that make up the playlist (`00218.m2ts`, `00217.m2ts`, `00212.m2ts`, …) now get demoted to `type='ignore'` automatically via `sync_duplicate_group_labels_for_disc`. Previously the sync only grouped titles with an exact `segment_map` match, so an mpls with `segment_map='218,217,212'` and its m2ts clips with `segment_map='218'`, `'217'`, `'212'` sat in different groups — the user had to individually ignore each m2ts (22+ clicks for a 39-title Harry Potter Blu-Ray, hit during Cell 7 of the smoke matrix). The sync now folds subsumed m2ts into their wrapper's group via the same clip-index logic path A / path B already use, so one decision on the primary covers the whole group.
- **Ripper Continue button no longer silently no-ops on failure (#204).** Three failure paths on the workflow-actions Continue button (resume prep, start prep, start transfer) previously logged the error to the console and reset the click state — but showed nothing to the user, making the button feel broken. All three now surface a toast with the actual error detail. Same treatment for the TransferConfig "Probe destination" button in Settings.
- **Library cold load no longer wastes 3 duplicate `/system/devmode` calls (#206).** Multiple services (shell, logger, workflow, frontend-version, setup guard) each subscribed to `SystemService.getDevMode()` independently, firing 3-5 identical HTTP requests on every cold page load. Now `shareReplay`-cached at the service — one HTTP call per session. Part of the wider Library-load fan-out issue tracked in #652 (v1.0.1).
- **Library cards now use TMDB posters by default (#647, #649).** Release cards on the Library page cascade `movie.cover_url` → `release.cover_front_url` → placeholder — previously they read only the release-specific box art from TheDiscDB, which sometimes rots / 404s and left the card blank. Boxset cards render `boxset.cover_front_url` only (a boxset is a *collection*, not a single title — a nested release's movie poster would misrepresent the card). The Ripper release-step preview is unchanged — it still shows the release-specific art users are attaching so they can verify what they added. `release.cover_front_url` remains a valid per-release override in the DB for enthusiasts who prefer edition-specific box art.

### Added

- **Ripper: "Already in Library" banner below the carousel for previously-ripped discs (#603)** — Inserting a disc that had already been ripped showed only an ordinary "Now Reading" drive card and the normal "Start copy" button — the system silently allowed a new rip with no indication that the disc was known. Added an `<app-already-in-library-card>` surface that renders *below the carousel* whenever the selected drive card represents a disc the user has already ripped through. The banner is passive: emerald `Ripped` pill, the joined release name, a one-line explanation, and a primary `Open in Library →` deep-link to the existing entry. The regular labeling + actions workflow stays visible underneath, so a deliberate re-rip remains one click away through the standard CTA — no separate "Re-rip" affordance on the banner (an earlier draft included one that just dismissed the banner to reveal the workflow; redundant). Backend signal: `DiscMetadata` gains `finalized` / `finalized_release_id` / `finalized_release_name` / `finalized_release_slug`, populated by `_build_disc_metadata` whenever the disc has a linked release AND either (a) has been formally finalized in the Library OR (b) has a completed job attached. The carousel additionally drops any stale job card for a disc_id that's already shown as a finalized drive card, so the user sees one clear surface. 4 new backend specs (`test_websockets_disc_metadata.py`) + 10 new frontend specs (7 in `already-in-library-card.component.spec.ts`, 1 dedup spec in `card-carousel.component.spec.ts`). Karma full suite: **880 SUCCESS**.
- **Search results: "In Library" chip on titles the user already owns (#590)** — TheDiscDB Search results showed no indicator that a hit was already ripped, so users had no way to tell at a glance. Added `POST /discdb/library-matches` which takes the result-title list and returns the subset that matches a movie in the user's library (case-insensitive, leading-article-stripping normalisation so "The Goonies" matches a "Goonies" library entry and vice versa). Frontend fires the lookup right after a successful search; matching cards get an emerald "✓ IN LIBRARY" chip in the upper-right corner. Soft affordance — the chip is fire-and-forget and the search list still renders if the lookup fails. 9 new backend specs covering normalisation, partial-string non-match, multi-result selectivity. Per-disc `Ripped` chip (contentHash match in the detail panel) deferred to a v2 follow-up.
- **`<ui-checkbox>` primitive (#595)** — Standalone, OnPush-friendly checkbox in `Frontend/src/app/ui/checkbox/`. Implements `ControlValueAccessor` so existing `[(ngModel)]="..."` callsites swap with only a tag rename. Visually-hidden native `<input type="checkbox">` underneath preserves keyboard focus, form-submission, and AT semantics; styled box overlay with check-icon SVG, hover/focus-ring/disabled states. 5 host-component specs.

### Changed

- **Setup: add optional TMDB API key step to the first-boot assistant (#614)** — Pre-1.0 the setup wizard had no surface for TMDB, so users had to discover it in Settings → TMDB after the fact (or never set it). Without TMDB the auto-suggestion on disc insert and the search on the film step stay hidden, hurting the first-hour experience. Added a new `setup-step-tmdb` component (modeled on the Discord step's optional-with-skip pattern) inserted at step 5 between Library (4) and Discord (now 6). Existing 0.x users have `first_time_setup_complete=true` and bypass the modal entirely, so the renumbering only affects fresh 1.0.0 installs. Uses the existing `POST /system/tmdb/config` endpoint from #610 — no backend changes.
- **Settings → Notifications: hide the per-category matrix when the Informative master toggle is off (#609)** — The Informative section had a master "Enabled" toggle plus a 6-row matrix of per-category × per-channel checkboxes. With the master off, the matrix stayed visible but greyed-and-pre-checked, reading as "enabled for the whole category, but they are all checked" — a confusing state disagreement that suggested something was wrong when the resolution logic was correct. Hidden the matrix entirely behind `*ngIf="informative.enabled"` and rewrote the section hint to explain that informational updates are low-priority and the matrix is for fine-tuning when enabled. Per-channel state stays in `categories[*]` so re-enabling restores the prior matrix exactly. Backend semantics unchanged (master OFF already blocked both channels for all informative levels at `core/notification_preferences.py:142`). 4 new Karma specs in the new `settings-page.component.spec.ts` covering both directions of the toggle and Action Required + Errors visibility regardless of the Informative state. Action Required + Errors sections unchanged — they don't have a master toggle so they don't exhibit the disagreement pattern.
- **Settings → TMDB: echo the persisted API key in the field to mirror MakeMKV registration (#610)** — Settings → TMDB rendered an empty input even when a key was configured. Adjacent Settings → MakeMKV echoed its registration key in clear text in the input. The asymmetry made the TMDB field look broken without delivering meaningful security — both keys live on the same disk under the same trust boundary. Aligned the two surfaces: `TmdbConfigResponse` gains `api_key: Optional[str]`; `GET /system/tmdb/config` and `POST /system/tmdb/config` both echo the persisted value so the UI can pre-populate and update the field without a re-fetch. `loadTmdbConfig()` binds `tmdbApiKey = res.api_key ?? ''` (was the unconditional clear); `saveTmdbConfig()` updates the field from the response. Input type flipped `password → text` to match MakeMKV (the password masking was a leftover from the never-echo posture). Pill ("Configured" / "Not configured") stays for at-a-glance status. 5 new backend specs in `test_tmdb_config_endpoint.py` (GET persisted, GET null, POST save, POST clear, roundtrip) + 4 new frontend specs in `settings-page.component.spec.ts`.
- **Title-editing surfaces: type-aware field cleanup + clips reposition (#602)** — Three small UX polish moves on the title-editing surfaces. (1) **Library disc drawer — Backdrop rows hide title / season / episode / edition** in both display and edit modes. Plex / Jellyfin name the file from the `backdrops/` folder it's in — the user has nothing to type. The chip + duration + Edit / Done + type select + description make the whole row; switching type away from Backdrop restores the inputs. (2) **Ripper title-editor — component clips section moves below the description and metadata footer** (was wedged between the duplicate-group panel and the description, now sits at the bottom just above the filename preview, where reference content belongs). (3) **Ripper title-editor — Episode rows hide title / season / episode inputs when TMDB returned a usable catalog** for the active season. The picker already auto-fills all three; the redundant manual inputs are noise. Loading / error / unavailable states keep the inputs so the user is never stranded. 7 new specs (4 in `library-disc-drawer.component.spec.ts` covering casing, display/edit hiding, type-switch restoration; 3 in `title-editor.component.spec.ts` covering clips DOM order, TMDB-aware hide, fallback when TMDB has no data).
- **Library disc drawer: compact-card title row redesign (#601)** — The 420px stacked-form layout repeated `TITLE` / `TYPE` / `EDITION` / `DESCRIPTION` uppercase labels above every input — only ~1.5 rows fit per viewport and the visual noise was overwhelming. Rebuilt as a 200px compact card: type chip + title input + duration on line 1, type select + edition on line 2 (or season/episode/type in series mode), description on line 3, file-path footer on line 4. Per-field labels dropped in favour of placeholders. Each row gains a 2 px left-edge accent bar colored by type (`cyan` for MainMovie, `indigo` for Episode, `amber` for Featurette / Interview / BTS / Deleted Scene / Extra, `blue` for Trailer / Clip / Sample / Scene / Short, `purple` for Theme Music / Backdrop, `slate` for Other), matching the chip palette. Cards now have proper borders + 12px gaps + hover lift instead of hairline separators. The disabled `⊘ Rename` button is removed from every row — restored when #325 wires v2. #531 itemSize contract preserved (200 == rendered height, regression spec updated). **Follow-up:** rows default to a display-only view (plain text + Edit button) and only switch to the editable input mode when the user clicks Edit — empty input boxes were still doing visual clutter even after the compact rewrite. At most one row is in edit mode at a time; clicking Done collapses back to display. The edition + description fields collapse into a single `edition · description` subline in display mode (hidden when both are empty), and unset titles render as a muted "Untitled". 10 new specs total in `library-disc-drawer.component.spec.ts`. Karma full suite: **859 SUCCESS**.

### Performance

- **Library disc drawer loaded in 30+ seconds on 300-title discs (#600)** — `GET /releases/disc/{id}` lazy-loaded `title_streams` mid-projection and shipped the full per-title JSON columns (`chapters`, `streams`, `metadata_scan`, `detection_flags`) plus the multi-MB `disc_info` scan cache on the `Disc` row — none of which the drawer reads. Applied the same `load_only` + `selectinload` playbook proven on the page endpoint by #530. Live measurement: 1917 UHD (309 titles) **30,729 ms → 390 ms (78.8× speedup)**, transfer 922 KB → 672 KB. The Goonies Blu-Ray (17 titles) 635 ms → 137 ms (4.6× speedup), 204 KB → 153 KB. `_disc_record` helper now passes `None` literally for the four heavy JSON columns so a future regression (a getattr on a deferred field) can't silently re-introduce N+1 lazy fetches. 3 new specs in `test_get_disc_record_perf.py` pin both the query-count bound and the lean-payload contract.

### Fixed

- **Setup: drives + already-loaded discs not detected until udev event after MakeMKV install / backend start (#613)** — On cold container boot with a disc already in the drive, the carousel rendered "Insert Disc" indefinitely; the user had to eject + reinsert or unplug/replug the drive to fire a udev event before the disc surfaced. Same for the post-MakeMKV-install drive list during the setup assistant — the carousel never populated until a manual refresh. Root cause: the backend warmup at `api/main.py:_deferred_startup_drive_warmup` + `core/makemkv_update_jobs.py` post-install both populate the disc cache via `handle_disc_insert()` per drive, but neither emitted a WebSocket event when the cache changed. Frontend coordinator had no signal to refetch. Added a `makemkv_drives_ready` WS event from both call sites with `{drives_count, source}`; `WorkflowService` routes it via a new `makemkvDrivesReady$` observable and auto-calls `syncCoordinator()` so the carousel refreshes without per-consumer wiring. Fix covers both "cold boot, disc already loaded" and "MakeMKV installed during setup, drives populate while user is mid-wizard" scenarios.
- **alembic: 202603180000_add_performance_indexes failed when an index already exists, leaving DB stuck pre-multi-drive (#612)** — Upgrading from a partial pre-1.0 schema where any of the 8 indexes the migration creates was already present (e.g. `idx_releases_boxset_id` from a manual perf hack) hit `DuplicateTable`, aborted the migration, and left alembic stuck at `202512311851`. Every subsequent migration silently skipped — including `202606170001` which adds `jobs.drive_by_id_serial` — and the running app then 500'd on every endpoint that joinedloads `jobs` (POST /system/transfer/configs being the most reachable). Switched all 8 `op.create_index(...)` calls to `op.execute("CREATE INDEX IF NOT EXISTS ...")` so partial schemas heal forward. Surfaced live during v1.0.0 smoke testing when adding an SMB transfer config returned 500.
- **Settings → Previews input styling absorbed by global form treatment (#608)** — The Preview duration field and Max parallel previews slider rendered with the legacy form styling (`padding: 0.75rem 1rem`, surface-2 background, 1rem font) instead of the intended dark-glass styling (`10px 12px`, `rgba(0,0,0,0.28)`, 13px font). Root cause: the global `input:not(...)` allowlist in `Frontend/src/styles.scss:635` has specificity (0,9,1) which beats the component-scoped `.settings-input { ... }` rule (0,1,0), so the intended `all: unset` chain was overridden. Same pattern that bit the disc-drawer in the #601 follow-up. Added `.settings-input` to the `:not()` allowlist on all three arms (base, placeholder, focus). Every settings tab using `.settings-input` benefits consistently — no per-tab regression risk.
- **Settings → Export/Import roundtrip restored + verified (#611)** — Surface a real bug while writing the verification suite: `serialize_database` and `deserialize_database` (`Backend/api/export_import.py`) referenced `db_models.BoxsetRelease`, a join-table model that doesn't exist. The Boxset → Release relationship is captured via `Release.boxset_id` directly (already in the exported `releases` table); the dead `BoxsetRelease` references would `AttributeError` the moment anyone actually exported a populated DB. Removed both the serialize site and the deserialize block; updated the summary dict accordingly. Second issue while validating in tests: `_model_to_dict` ISO-formats datetimes for JSON safety, and psycopg2 (production) silently coerces ISO strings back into TIMESTAMP columns — so the legacy path "just worked" without explicit revival. SQLAlchemy's SQLite driver (CI / tests) does not, raising `TypeError: SQLite DateTime type only accepts Python datetime and date objects`. New `_revive_row` helper detects ISO strings on datetime/date-typed columns at import time and converts them back; wired into all six per-table import sites. New `test_export_import_roundtrip.py` covers: all seven export tables populated; end-to-end roundtrip with FK chain validation (release→movie, release→boxset, disc→release, job→disc, title_stream→title); preserves #607's `file_path` / `file_path_stage` values through the roundtrip; idempotent re-import skips all rows; partial payload (single table) accepted; orphaned release (missing movie_id) is skipped not inserted.
- **Library: disc panel rendered "Path unknown" / "In transient" for transferred titles — two compounded bugs in the file_path pipeline (#607)** — **Writer bug:** `_update_title_file_paths_after_transfer` (`Backend/api/routers/jobs.py:3693`) walked the destination via `Path(dest_root).rglob("*.mkv")` to map files back to title_ids. That only works for local-mode destinations; for SMB (`smb://host/share/...`), rsync (`user@host:/path`), and NFS URIs, `pathlib.Path` can't walk anything and the title-id map stayed empty, so `DiscTitle.file_path_stage` never advanced from `'postprocess'` to `'transfer'`. **Projection bug:** `_disc_record()` (`releases.py:461-500`) was already eager-loading `file_path` + `file_path_stage` (via `_disc_record_load_options` at `releases.py:2388-2389`) but the `DiscTitleRecord(...)` constructor silently dropped both fields — every drawer row therefore fell through to the default "Path unknown" label regardless of what the DB recorded. Live evidence on a real install pinpointed the writer regression to mid-April 2026: 50 completed SMB transfers between 2026-06-18 and 2026-06-27 had **zero** rows at `file_path_stage='transfer'`; 126 historical rows from 2026-03-21 to 2026-04-17 (when the writer still worked) correctly had `smb://...` URIs stage-tagged. After fixing the writer, the projection bug masked the recovery — the API still returned `None` for both fields against a confirmed-correct DB. Rewrote the writer to construct per-title destinations from `Job.post_paths` (which already carries `{title_id → relative_path}`) concatenated to `dest_root` — no filesystem stat'ing, trailing-slash normalisation, single-file (`.mkv` suffix) matched by basename. Added `file_path` + `file_path_stage` to the `_disc_record()` serializer. New one-shot back-fill script at `scripts/backfill_transfer_file_paths.py` replays the now-fixed writer against every completed transfer so in-the-wild stuck rows catch up (validated against the live install: 444 rows now at `file_path_stage='transfer'`, up from 126; 50 jobs back-filled, zero failures). 10 new specs total: 9 in `test_update_title_file_paths_after_transfer.py` covering all four modes + edge cases (trailing slash, single-file basename match, idempotent re-run, missing post_paths/dest_paths), plus 1 round-trip spec in `test_get_disc_record_perf.py` asserting the endpoint actually returns the two fields across rip / postprocess / transfer (local + smb + rsync) / no-path stages.
- **Ripper: transfer step strands the UI + emits a misleading "Ready to transfer" toast (#605)** — Two compounded symptoms on the transfer step. (1) The CTA appears to stop after post-process: clicking it kicks off prep, the user gets a "Ready to transfer" notification, and the UI then stays on the transfer card as if nothing else happened. The backend actually chains post-process into transfer automatically via `_maybe_auto_dispatch_local_transfer` / `_maybe_auto_dispatch_remote_transfer` (running in the same Celery worker), but the in-process progress emit at `Backend/workers/tasks.py` four sites shipped a payload that lacked `post_state` and `transfer_state` — so the frontend never learned the transfer transitioned `ready → running` and the CTA button label stayed in pre-prep mode. (2) Frontend stays stuck on the transfer view after transfer completes (sibling of #604) — same shape: the final `transfer_state=completed` write was only visible to the frontend via the debounced `context_changed` refetch, never via the progress channel. Fix: completes the #604 pattern — every `progress_data` emit site now ships `rip_state`, `post_state` (read via `derived_post_state` hybrid since the column was dropped in #365), and `transfer_state`. `ProgressUpdateMessage` (workflow.service.ts) gains the two new fields; the progress-update handler propagates them with the same `??`-chain fallback so older payloads / mid-rip ticks preserve existing state. Additionally, the intermediate "post-processing complete. Ready to transfer." notification (emitted from `core/job_state.py`) is now suppressed when the active TransferConfig is auto-dispatch-capable (`local`, `rsync`, `smb`, `nfs`) — the user sees a single terminal "Transfer complete" / "Job complete" toast at the actual end, not two messages for one auto-dispatched workflow. 1 new backend spec in `test_rip_progress_in_process.py` + 5 new specs in `test_local_transfer_progress_emit.py` and `test_postprocess_notification_suppression.py` (the latter as a new file covering `_auto_dispatch_will_run` for local/rsync/smb/nfs/none/unknown/exception paths) + 4 new frontend specs in `workflow.service.spec.ts`. Karma full suite: **884 SUCCESS**.
- **Ripper: verifying spinner stuck after rip copy completes — required page refresh to advance (#604)** — When a rip finished copying, the frontend transitioned into the verifying state and never advanced. Root cause: the in-process rip-progress callback (`_post_rip_progress` in `Backend/workers/tasks.py`) calls `apply_job_state(..., skip_context_changed=True)` — which suppresses the authoritative `context_changed` WS event — and the `progress_data` dict it emits to the frontend omitted `rip_state` entirely. So when verification flipped `rip_state` from `running` → `completed`, the frontend's local `jobStatus.rip_state` stayed at `running` and `calculateStageProgress` kept rendering "Verifying…" indefinitely (gate at line 5384: `ripProgress >= 100 && ripState !== 'completed'`). A hard refresh recovered because the full HTTP context load reads the latest stage state. Fix: all four `progress_data` emit sites in `tasks.py` (rip-progress live, in-process post-rip, transfer-progress, and the local-transfer dispatch path) now ship `rip_state`. `ProgressUpdateMessage` (workflow.service.ts) gains `rip_state?: string | null`; the progress-update handler propagates it onto the local `jobStatus.rip_state` with `??` chain fallback so older payloads / mid-rip ticks preserve existing state and never clobber it. 2 new backend specs in `test_rip_progress_in_process.py` + 3 new frontend specs in `workflow.service.spec.ts`. Karma full suite: **881 SUCCESS**.

- **Library disc drawer: 57/97 auto-ignored titles drowned out the 40 the user actually needed to edit (#599)** — Drawer rendered all titles flat — typical Blu-ray has ~59% auto-ignored junk that pushed the 2 MainMovie / named-extra titles below the fold. Lifted the production "show ignored" toggle pattern from `title-label.component` into the drawer (`showIgnored`, `ignoredCount`, `toggleShowIgnored()`). Visible titles now sort by type priority — MainMovie → Episode (season/episode-aware) → Featurette / Interview / BehindTheScenes / etc. → Other → Unset — with duration descending within ties. Ignored rows tuck at the end of the sort, hidden by default; the `Show ignored (57)` button reveals them dimmed so the user can un-ignore false positives without losing access. PATCH dispatcher reads from the canonical `allTitles` so editing an ignored row works whether revealed or not. 6 new specs in `library-disc-drawer.component.spec.ts`. Karma: **849 SUCCESS**.
- **Library disc drawer: panel too narrow at 480px (#598)** — Bumped to `min(720px, calc(100vw - 40px))`. 50% of a 1440px viewport with 40px breathing room; mobile (≤720px) breakpoint still goes full-width.
- **Library page header had a dark stripe that clashed with the cyan-tinted release cards (#596)** — Dropped `rgba(0,0,0,0.4) + backdrop-filter blur + border-bottom` from `.library-header`. Sticky positioning preserved; the header now flows visually with the rest of the page like the Search header does.
- **Library: boxset child releases displayed the boxset's name instead of the underlying movie's title (#597)** — A Harry Potter boxset rendered five rows of "Harry Potter 8-Film Collection (year)" instead of "Harry Potter and the Goblet of Fire (2005)", "...Order of the Phoenix (2007)", etc. Root cause: `_release_summary()` set `name = boxset.name or rel.name` for boxset children, even though `rel.movie.name` was already eager-loaded. Now: boxset child rows use `rel.movie.name` for display, the edition string moves to `release_name` (the schema's existing-but-unused field) for callers that need both. Standalone releases unchanged. 3 new specs in `test_boxset_relationships.py`.
- **Settings → Transfer history rows showed raw job UUIDs (#593)** — Each row's header was the raw job UUID (e.g. `baa1e4b4-6ac4-…`), forcing the user to cross-reference Library to identify what had been transferred. Backend `get_transfer_history` now eager-loads the `Job → Disc → Release → Movie` chain (single query, no N+1) and surfaces `movie_name` / `release_name` / `release_year` / `disc_name` on `TransferHistorySummary`. Orphaned rows (job deleted with `ON DELETE SET NULL`) return NULL identity fields, get an "orphaned" chip in the UI, and degrade gracefully to source-path parsing then to the raw UUID. The first-line title now reads as `Movie Name (Year) — Disc Name`; the UUID is preserved as a small monospaced subtitle for support / debugging. 2 new backend specs (full chain + orphan), 5 new frontend title-resolution specs. Karma: 843 SUCCESS.
- **Settings → Transfer history KPI tiles always read zero (#592)** — Total Transfers / Successful / Failed / Avg Speed tiles always rendered `0` while the list below showed dozens of completed transfers. Root cause: KPIs came from `GET /system/transfer/statistics?days=30` (server-side aggregate over a hardcoded 30-day window) while the row list came from `GET /system/transfer/history?limit=100` (no day filter). When the most recent transfer fell outside the 30-day window — easy on a media-server install that rips in bursts — the aggregate returned zeros while rows still rendered. Dropped the separate stats call entirely; KPIs are now derived client-side from the same `filteredHistory` array the rows render, so the four numbers always agree with what's visible and respect the active Config + Status filter. Avg Speed excludes failed rows and zero-/null-speed rows. Added a "Stats based on the N most recent transfers loaded" footnote to set expectations about the `limit=100` window. 5 new specs in `transfer-history.component.spec.ts`.
- **Settings: raw browser checkboxes on Notifications + MakeMKV (#595)** — The Notifications matrix (informative master toggle + every per-category × per-channel cell, plus the action-required and errors blocks) and MakeMKV's "Include ffmpeg" option used unstyled native `<input type="checkbox">`, which rendered as the OS default light-gray box and looked unfinished next to the polished controls elsewhere. Swapped all callsites to the new `<ui-checkbox>` primitive (CVA-backed, so `[(ngModel)]` bindings transferred 1:1). Karma full suite: 833 SUCCESS.
- **Settings → Previews slider range broken when persisted value exceeds browser-host CPU count (#594)** — The `Max parallel previews` slider's `[max]` was bound to `navigator.hardwareConcurrency` (browser CPU count), but the persisted value was written by the backend using `os.cpu_count()` (server CPU count). On a host where the browser saw fewer cores than the server, the slider thumb could not reach the saved value (observed: "8 concurrent" with the slider track ending at 6). Made the backend the single source of truth: `PreviewSettings.max_parallel_ceiling` (read-only, sourced from `os.cpu_count()`) drives the slider's `[max]`; `get_preview_dict` clamps any oversized persisted value on read so existing settings.json files display correctly without a migration; `POST /system/preview/config` returns HTTP 400 if `max_parallel > ceiling` instead of silent coercion; schema-level hard cap `le=128` defends against absurd writes. 7 backend tests (3 new schema/clamp specs + 2 new HTTP route specs + extended GET response shape spec).
- **Ripper carousel job-card spinner stayed on for at-rest / failed / between-stage jobs (#591)** — `isProgressProcessing` treated any stage `< 100` as "processing," so a job sitting at "awaiting labeling" (post_progress=0) or between any two stages spun forever. Tightened to strictly `(0, 100)` per stage, with `rip_phase` covering the pre-heartbeat moment when rip just started. Also added a terminal-status short-circuit in `isJobCardProcessing` so failed / completed / cancelled jobs never spin even if their last-known progress message would suggest otherwise. 8 new unit tests in `card-carousel.component.spec.ts`.
- **Library drawer scrolling + mobile cards (#531 — #533)** — The disc drawer's virtual scroll declared `itemSize=160` against ~418px real rows, leaving the bottom half of long title lists unreachable (26-title disc: 4,160px of scroll space for ~10,900px of content). Now itemSize=420 with the row enforced as a fixed 420px box and a spec pinning rendered-height == itemSize + content-fits-box. Mobile (≤560px) release/boxset cards stop crushing titles to a ~90px ellipsis: actions wrap to their own row, titles get a 2-line clamp, meta stays on one line. Desktop unchanged.
- **Library page load: 9.2s → 0.8s (#530 — #532)** — `GET /releases/library/page` lazy-loaded `disc.title_streams` per disc to compute `label_present` (37 queries), hydrated every Disc column (`disc_info` scan cache = 5.8MB of the table) and every DiscTitle JSON column for a 14-field projection, and shipped ~1.1MB of inline `titles` arrays the page only read `.length` from. Now: one batched title_streams lookup per page, `load_only` on the discs + titles selectin legs, and a new `DiscSummary.title_count` with the page opting out of inline titles (`GET /releases/{slug}/discs` keeps the full #380 projection unchanged). Measured: 9.21s/52 statements/1.1MB → 0.82s/17 statements/185KB.
- **`GET /releases/disc/by-hash` 500 for discs with titles (#528 — #529)** — #380/#500 tightened `DiscSummary.titles` to the typed `TitleSummary` projection, but `get_disc_by_hash` still passed `DiscTitleRecord` dumps keyed `id` (and raw scan-payload dicts in its fallback), so any disc with persisted titles failed validation. Now reuses the Library's `_build_title_summaries` projection and routes the scan-payload fallback onto `DiscSummary.tracks`, the field documented for that payload shape. Regression test re-enabled + fallback-branch test added. Closes #528.
- **Detection deferred until post-rip-verification; clean re-runs heal stale auto-ignores (#518, #517)** — The during-rip incremental preview dispatch chained `detect_raw_titles` per title batch, so ffmpeg junk-detection ran while MakeMKV was still hammering the same disk (observed live on the Fallout S2 re-rip, 2026-06-10: ~7 detect passes during a 4-hour rip; an early pass under I/O starvation produced a false-positive junk verdict on a 52.8-min episode and auto-ignored it). `preview_raw_titles` now takes `chain_detect: bool = True`; the during-rip per-title dispatch (`workers/tasks.py` rip-progress parser) passes `chain_detect=False` — incremental previews keep arriving for early labeling, but detection runs exactly once per job via the post-rip-verification preview pass (`rip_verification_complete_callback` miss branch), which keeps the default. Folded in #517: detection's auto-ignore was sticky — a later clean run overwrote `detection_warning` to false but never reverted the ignore. `run_detect_raw_titles_phase` now clears the stale detection-sourced auto-ignore when a re-run computes `detection_warning=false`, gated so it never touches user decisions (`user_type` set), obfuscation-driven ignores (`obfuscation_reason` set), m2ts subsumption (`subsumed_by_title_id` set), or demoted duplicate secondaries (`active=false`). After #518's deferral the heal path matters mainly for the detection-regenerate endpoints. 8 new tests in `test_detect_deferral_and_stale_ignore.py` covering the chain flag (both dispatch shapes + a source-level guard on the during-rip call site), the heal, all four guard conditions, and the preserved positive-verdict path.
- **Duplicate-group consensus-fill no longer self-votes ignore onto NULL primaries; postprocess `_rename_movie` falls back to `Track{tid}` instead of colliding on `movie_name`** — Two-layer fix surfaced on Fallout S2: every episode's `.mpls` primary was auto-marked `auto_type='ignore'` (and the UI hid them via the title editor's `*ngIf="!isIgnored()"` gate, which also blocked the TMDB episode picker for the same rows). Upstream: `apply_primary_duplicate_row` in `Backend/core/duplicate_group_sync.py` was filling NULL primary types with `'ignore'` whenever every sibling already had `type='ignore'` — but that condition was always trivially true because `apply_secondary_duplicate_row` runs on the same pass and sets `type='ignore'` on the demoted siblings as a structural consequence of picking THIS row as primary. The siblings' "vote" was circular; every 2-row duplicate group (one `.mpls` playlist + one `.m2ts` clip per episode) got its primary hidden. Downstream: the consensus-fill existed only to paper over a `_rename_movie` collision — its NULL-typed fallback chain ended at `movie_name + (year)` "to avoid Track{tid}", which silently collapsed N NULL primaries onto the same `Movie (Year).mkv` and overwrote each other (the "17/20 collision"). Removed the consensus-fill entirely (now a no-op; `fill_null_type_from_consensus` parameter kept on the signature for back-compat with per-patch sync callers) and dropped the `_rename_movie` NULL→`movie_name` fallback so NULL-typed titles fall straight through to `Track{tid}` — ugly but unique, matching `_rename_series`'s already-correct behavior. The main-movie branch (gated on `title_type in {mainmovie, main, movie}`) keeps `movie_name + (year)` because it's structurally safe (at most one main per disc). Validator `_validate_all_titles_labeled` already accepted NULL primary + ignored siblings as labeled, so no validator change needed. Three existing tests in `test_duplicate_group_sync.py` enshrined the buggy consensus-fill behavior — rewritten to assert NULL primaries stay NULL when only demoted secondaries surround them, and renamed accordingly. New `test_rename_movie_null_type_no_collision.py` regression-guards the collision: 4 NULL-typed titles must produce 4 distinct `Track{tid}` filenames.
- **Ripper carousel — pending + failed cards survive nav-back (#498)** — Navigating Ripper → Settings → Ripper used to wipe `pending` and `failed` job cards from the carousel until a full page refresh. Root cause: the coordinator's WS `initial_state` snapshot (`_build_initial_coordinator_state_sync` in `Backend/api/routers/websockets.py`) filtered jobs by `job_status in {running, validating}`, while the HTTP `/jobs/unfinished/summaries` endpoint used at first load also includes `pending` plus latest-per-disc failed (excluding superseded by a newer active job or a completed re-rip). On nav-back the frontend's `request_sync` triggered a snapshot that dropped the broader set, and the `initial_state` handler blanket-overwrote `_discs` with it. Extracted the shared query into `Backend/api/unfinished_jobs.query_unfinished_jobs(db)` and pointed both code paths at it so the WS snapshot and the HTTP summaries always agree.
- **Local-transfer progress bar updates live (#499)** — The auto-dispatched local transfer's progress callback in `Backend/workers/tasks.py:_maybe_auto_dispatch_local_transfer` wrote `transfer_progress` to the DB but did not emit a WebSocket event, so the frontend's transfer bar appeared frozen until a page refresh jumped the UI to its terminal state. Added an `emit_job_progress_debounced(job_id, {…, "transfer_progress": pct})` call after `apply_job_state`, mirroring the existing remote-transfer pattern (`_post_transfer_progress`). The completion/failure paths already emit via `set_status`, so the running-state emit is the last gap. `_hash_progress_cb` delegates to `_progress_cb` so the fix covers both transfer-bytes and hash-verification progress.
- **Staging baseline test suite (#391 sweep)** — Cleared the pre-existing test-failure surface on the post-#390 staging tip so the postprocess→transfer collapse (Phase 2) can resume against a green base. Fixed: workflow-context PATCH cache-invalidation race (a save followed by a re-read could return the pre-save snapshot, e.g. echoing a cleared `movie_id` back; `Backend/api/routers/jobs.py:save_job_workflow_context`); WorkflowService spec used `r.url.includes('mount_point=...')` even though the service now puts the param on `HttpParams`; LabelShellComponent spec missing `provideHttpClient()` for transitive TitleLabelComponent injection; HistoryPageComponent spec missing `getLibraryPage` stub on MetadataService (component now uses the paginated endpoint); 8 disc-lock test calls aligned with new `_is_makemkvcon_running_for_operation(operation_type, *, disc_num=...)` signature and `get_disc_lock_debug_snapshot()`'s renamed `"key"` field; 7 ffmpeg-detection tests now use `f.touch()` so the production `path.stat()` prefer-on-disk-size guard falls back to the test's `size_bytes` hint; 4 workflow-context-titles fixtures gained `stage_profile='miss'` and `release_link_ready` fields; E2E fixture `e2e_test_environment` intercepts worker HTTP callbacks via the shared `stage_callback_intercept` fake so worker `requests.post()` no longer hits a host-side container on port 8000 and gets a 403. 23 ticketed child issues (#392–#414) + 7 follow-up child issues (#416–#422) closed or tracked. The `optical_change_inserts` udev test fixture had a lambda that crashed pytest's report builder during failure formatting; `**kwargs` accepted so per-test failures no longer take down `pytest_sessionfinish`. Two known-hang integration files quarantined (#416, #417) pending the Phase 2 rewrite.
- **Path A segment-reorder** — After the user submits an ordering and the backend resolves a canonical playlist, the exploratory rip's `.mkv` (typically ~37 GB on Midway-class discs) is removed from `raw/` and dropped from `job.ripped_files` before the canonical rip is dispatched. Skipped when the exploratory title turns out to BE the canonical (the file we already have is the rip we want); in that case the canonical index is excluded from `rip_set` so the worker doesn't re-rip it. When the exploratory was canonical AND there are no extras left to rip, the API short-circuits the canonical rip dispatch entirely and advances the job straight to post-rip via `StageState.rip_complete`.
- **Threshold modal (Path A)** — The "Rip whole disc anyway" choice is now disabled with a clear "not enough free disk" hint when projected size exceeds free space at `MKVAUTO_DATA` (modal payload now includes `available_disk_bytes` populated from `shutil.disk_usage(get_mkvauto_data()).free`). The modal also keeps a pending state during `POST /jobs/rip-with-segment-reorder`, so the click registers immediately and the modal only closes once the request resolves (fixes a click-twice-required UX regression).
- **Drive carousel scanning spinner** — Moved from the inline eyebrow row (12px, awkwardly placed) to the absolute top-right corner (16px, blue), matching the existing `disc-card-job-spinner` slot and the prototype `DriveCard`.
- **Ripper / disc workflow** — `GET /discs/workflow-context` (by mount) now prefers persisted `disc_titles` labeling (`type`, title text, description, season/episode, edition) over stale `disc_info.titles` from the drive cache, so choices such as **ignore** stay visible after refresh when TheDiscDB had pre-filled types.
- **Ripper / label complete** — `POST /jobs/{id}/label/complete` applies `labelForm.tracks` the same as `titles` when persisting title rows before validation. The workflow UI only sends `tracks`, so the backend used to skip the final merge and could reject **Continue** even when the form looked fully labeled. The error toast now shows the server message when present (e.g. validation detail) instead of only a generic line.
- **Ripper / label complete** — `_validate_all_titles_labeled` now matches the title step UI: when a duplicate **segment_map** group has exactly one `active=True` primary, other members in that group are not required to pass per-row name/type checks (avoids false **Cannot complete labeling** on duplicate secondaries with cleared or stale metadata).
- **Transfer / SMB** — Directory uploads use full remote paths (`put` / `ls` from share root) instead of `cd` + basename `put`, and non-benign `mkdir` failures abort the transfer instead of uploading MKVs to the share root. Post-process resume only skips rename when transient has MKVs **and** raw has none (avoids skipping rename when both trees still have files).
- **Ripper / releases** — Linking a newly created release no longer treats the canonical `release_slug` as the edition display name, so workflow saves do not overwrite `releases.name` with a sluggified string; the API also ignores label payloads where `release_name` equals `release_slug`.
- **Plex / extras paths** — For `media_server=plex`, title types that only have distinct Jellyfin extras folders (Extra, Sample, Clip, ThemeMusic, Backdrop) are placed under the movie’s **`Other`** folder instead of **`Extras`**, since Plex does not treat a top-level `Extras` folder like Jellyfin’s layout. Jellyfin subfolder names are unchanged; labeling hints describe the Plex behavior.
- **Notifications** — Pipeline toasts and Discord copy that append “Disc #…” now prefer `discs.disc_number` over `jobs.disc_num` when the disc row has a value (fallback unchanged when unset).
- **Notifications** — Removed redundant client-only “Label stage completed” toast after `POST /jobs/{id}/label/complete`; the existing pipeline `label_complete` notification already covers labeling finished.
- **Start rip / copy** — Removed the 30s client timeout on `POST /jobs/rip` (slow API responses no longer look like failures). Ambiguous network/gateway errors show guidance to refresh the page; the app refetches job-by-disc when possible to recover state.
- **Rip notifications** — Rip complete toasts use the same `apply_job_state` payload as the verification-complete transition (so the toast is not skipped when ORM phase/post_state lag), with a generic fallback if state is unexpected; terminal “Job complete” uses Discord level `job_completed` so it does not collide with rip milestone dedupe.
- **Ripper carousel** — Ejecting a disc no longer rewrites unfinished/failed job cards that share the same `mount_point` into empty “Insert disc” drive slots.
- **Remote transfer (Celery)** no longer crashes on progress updates (`transfer_remote` called the rip-progress helper with invalid arguments); copy progress is reported via `POST /jobs/{id}/transfer-progress` instead.
- **Rip verification** waits for raw MKV size **quiescence**, runs an **ffprobe** readiness check, then hashes and syncs **`mkv_size`** so partial writes are less likely to poison `source_hashes` / DB sizes; post-process **pre-flight** no longer requires exact on-disk size equality with `mkv_size`.
- Failed jobs attach to the **Now Reading** in-drive carousel card when the disc is inserted (coordinator metadata, `job_finished`, dedup carry, summaries hash skip) with a visible Failed state on the drive card.
- Ripper carousel hides superseded failed job cards when a newer active job exists for the same disc (API + UI filter).
- Release cascade deletion (discs no longer deleted with release; orphan cleanup only when safe).
- Duplicate release prevention (DB constraints and locking).
- Workflow CTA buttons now show spinner and are disabled during all Continue/advance actions (not just Start Rip).
- HTTP timeouts (30–60s) on workflow calls prevent stuck UI when backend is slow.
- In-drive card no longer disappears when job completes (card stays with completed badge).
- DiscDB scan now emits options_changed so movie/release selectors update without reload.
- MakeMKV installed version cached by binary stat (no more `strings -a` on every health check).
- MakeMKV key expiration warning on startup (toast notification if key is expired).
- Loading Content card now matches carousel/workflow width.
- Eject disc on finish setting (disabled by default); eject all discs on restart setting.
- Failed/incomplete job cards persist in carousel with dismiss (╳) button.
- Rip timing: `rip_started_at` and `rip_completed_at` timestamps recorded on jobs.
- Setup modal auto-dismisses when setup completes in another browser tab.
- Backward compatibility policy and upgrade guidance in VERSIONING.md.
- Elapsed rip time display on progress bar (desktop and mobile) when rip is active.
- Transfer configuration help text in Settings explaining Local/rsync/SMB/NFS modes.
- Postprocess progress callback endpoint (`POST /jobs/{id}/postprocess-progress`) for worker→API migration.
- System requirements documentation (minimum, recommended, heavy workload specs; Celery worker tuning).
- Rip validation: fail rip when important (non-ignore) titles are missing from output.
- MakeMKV registration status cached (5-min TTL) for faster setup modal and health checks.
- Rename Files button on workflow transfer step (preview + apply renames before transfer starts).
- Remote transfer (rsync/SMB/NFS) now runs in Celery worker, not the API process. Eliminates DB connection pool exhaustion during long transfers.
- DiscDB contribution tracking: database columns + API for managing disc submissions (Phase 1).
- Disc-scoped durable preview storage paths and serving routes (foundation for preview persistence across jobs).
- Configurable path templates for movie and series output paths (`path_template_movie`, `path_template_series` settings).
- Various workflow, UI, and WebSocket fixes for labeling, progress, and mobile layout.

### Added

- **Auto-rip on insert (#331 — #526)** — New "Auto-rip on insert" toggle in Copy Settings. When enabled, a completed disc scan dispatches the rip automatically through the same path as the Start Copy button, so the Path A modal gate, gatekeeper duplicate protection, and disk preflight all apply unchanged. DiscDB hits run as normal hit jobs; misses rip first without a linked release and land on the `film` step for post-rip linking via the existing labeling flow (`complete_label` keeps blocking postprocess until linked). Unhashed discs never auto-rip; Path A discs needing a rip-mode choice skip with an action-required notification instead. Closes #331.
- **Return to titles with pipeline edit guards (#363 Horizon 1 — #525)** — Users can navigate back to labeling steps and fix mistakes (rename, retype, un-ignore) until the pipeline consumes the labels. New backend guard on the title PATCH/batch/ops routes rejects with 409 `labels_locked` (any edit once label is complete or postprocess is running/finished on the active job; completed jobs stay editable) or `type_change_locked` (un-ignoring a title a selective rip never produced — all-mode rips and in-`rip_set` titles keep un-ignore allowed). The frontend mirrors the lock: backward navigation into labeling steps is gated and rejections surface as toasts. Horizons 2–3 (relabel after postprocess, post-finalize policy) split to #527. Closes #363.
- **Exportable DiscDB contribution bundle + documented manual process (#85, #86 — #524)** — The contribution bundle endpoint (`GET /discdb/contributions/{disc_id}/bundle`) imported a function that never existed, so every call 501'd. Now implemented as an in-memory builder reusing the finalize JSON shapes (`release.json` / `discNN.json` / summary text) with graceful degradation when job artifacts are gone (`info_log_included: false`). Successful exports stamp `discdb_exported_at` and promote the contribution status to `exported` (never downgrading `submitted`/`accepted`). The Library disc drawer gains a one-click **Export DiscDB bundle** button for DiscDB-miss discs, and `docs/Guides/DISCDB_CONTRIBUTION_CHECKLIST.md` documents the bundle workflow end-to-end (UI + curl + upstream file mapping + status tracking). Auto-stream contribution remains a v2 design. Closes #85, closes #86.
- **#374 integration coverage + obfuscation reason-tier doc sync (#523)** — Adds the missing end-to-end test for the post-ffprobe duration-sanity obfuscation source (`detect_raw_titles` with a stub raw file: short-declared/long-actual flags `duration_short` and corrects the duration; relational reasons are never downgraded; sub-threshold rows untouched), pins the dedupe-overwrites-post-ffprobe precedence as intentional, and syncs the `obfuscation_reason` tier comments with the shipped `duration_short` / `low_bitrate_decoy` sources. Closes #374.
- **#366 regression coverage — `validating` leak (#522)** — Two regression tests completing the issue's acceptance criteria: a stale `job_status='validating'` entering hit-profile `_complete_transfer` must finish the job as `completed`, and startup recovery must sanitize an already-transferred job (`transfer_state='completed'` + missing local `transient/`) **before** the job-dir-missing branch can fail it. AC3's optional remote re-verification on recovery is documented as out of scope — remote modes intentionally skip file-level verification. Closes #366.
- **Matroska Segment UID capture at postprocess (#448)** — Every MKV produced by MakeMKV carries a random 128-bit Segment UID in its container header; this PR reads it via `mkvmerge -J` immediately after `_update_title_file_paths` in `_run_prep_phase` and persists it on a new `disc_titles.segment_uid` column (nullable + indexed, Alembic migration `202605300000`). Pure read — no file modification, no XML tag schema. Capture is wrapped in `try/except` and the per-file failures inside the new `core.mkv_identity.read_segment_uid` are logged-only, so a missing `mkvmerge` binary, a corrupt file, or an unreadable path can never turn a successful postprocess into a failure — the row just stays `NULL`, which downstream consumers treat as "fall back to heuristic match". Pulled forward from #449 (v2 self-healing reattach) because the transient/-drop workstream's `src == dest` short-circuit (5b'b) needs file identity that survives renames between postprocess and transfer; `post_paths` is fragile to library reorganisation but the container UID is intrinsic to the file. `mkvtoolnix` added to the Docker image (~3 MB installed) next to `ffmpeg`. Legacy titles produced before this PR stay `NULL` by design — no backfill. 8 new tests in `test_mkv_identity.py` (read happy path, missing binary, non-zero exit, malformed JSON, missing field, subprocess raise, write-column happy path, skips-on-None read) plus a rendezvous extension in `test_post_paths_lifecycle.py` verifying `file_path` and `segment_uid` land together in the same DB session.
- **Postprocess→Transfer collapse — Phase 2 scaffolding (#325, #365 in progress)** — Adds the surface area for the architectural decision recorded in `docs/ADR-001-postprocess-collapse.md` (Option B: rename + hash + validate move into the transfer stage's *Preparing* sub-phase). Lands as a deliberately partial step — `post_state`, the `postprocess-complete` callback, and the `'postprocess'` WorkflowStep value are all preserved so the existing flow keeps working through the transition window. New `Job.transfer_phase` column (`"preparing" | "transferring" | "verifying"`) via Alembic migration `202605290000`. New `start_transfer` Celery task — currently delegates to the existing `resume_postprocess` body so prep logic stays in one place, but becomes the canonical entry point going forward. New `POST /jobs/{id}/transfer/start` endpoint with rip-complete + label-complete + not-already-transferring preconditions. Five auto-progression call sites in `Backend/api/routers/jobs.py` rewired to enqueue `start_transfer` instead of `resume_postprocess` (rip-verification-complete success + heal, rip-complete heal, manual `POST /postprocess`, Path A canonical completion). `resume_postprocess` itself stays registered as a deprecated entry point so recovery paths (`resume_failed_postprocess`, `core/failure_recovery.py` auto-retry) and any in-flight Redis-queued legacy tasks across a deploy continue to execute; it now defensively sets `transfer_phase="preparing"` so the UI sub-phase indicator works regardless of which entry point fires. Frontend `JobStatus.transfer_phase` threaded through the schema + `get_status` + `workflow.service.ts` `transferPhaseLabel`: `transfer_phase="preparing"` → "Preparing files…", `transfer_phase="verifying"` → "Verifying integrity…". Legacy fallback (transferState-based heuristic) preserved for jobs that predate the collapse. **12 new tests** in `Backend/tests/test_start_transfer_endpoint.py` covering endpoint preconditions, schema surface, and task-name registration safety. Phase 2 PR #425. The follow-up cleanup PR will extract the prep body into a shared helper, shrink `resume_postprocess` to a thin forwarding shim, drop the `post_state` column, and collapse the `WorkflowStep` enum.
- **Titles step — source-aware chip system + unified duplicate collapse + DuplicateGroupPanel polish (6 PRs)** — Splits `disc_titles.type` into `auto_type` (DiscDB import / scan-time / Path A sibling-ignore / m2ts subsumption / FFmpeg padding-detect / dedupe-group consensus / MakeMKV decoy flag) and `user_type` (PATCH edits / exploratory-rip canonical selection / "previous order had decoys" / Confirm-ignore button / labelForm save). New `crud.set_title_type(title, value, source)` is the single chokepoint for writes; 12 existing write sites refactored to route through it so a generic dict-spread setattr can't desync the columns. Effective `type` stays as the denormalized `user_type ?? auto_type` cache; legacy readers unchanged. Migration `202605230001` backfills `user_type = type` for every existing row (conservative — treats legacy edits as user-initiated). The chip system on every title row now renders 0-2 chips per the matrix: **User selected** (emerald) when `user_type` is non-ignore, **DiscDB** (cyan) when `auto_type` is non-ignore, both when they agree, **Pending Review** (amber) when neither has touched the row, **Ignored** (slate) when `user_type='ignore'`, and *blank* when only auto flagged ignore (user reviews via the right-editor's amber **Confirm ignore** CTA). Show-ignored toggle gates user-confirmed ignores only — auto-ignored rows stay visible chip-less. New `TitleChipsComponent` + `computeTitleChips` helper export the pure matrix logic for reuse. Real left-rail sibling collapse: `core/duplicate_info.attach_duplicate_info` switched from order-preserved keying to sorted-segment-set keying so Midway 4K's 175 mpls permutations finally share one `group_id` instead of relying on hide-via-ignore. Subsumed m2ts (titles with `subsumed_by_title_id` set) are absorbed into the wrapper's group via a post-pass walk — orphan m2ts (no subsumed_by) untouched, preserving the Rick and Morty TV-extras case (raw m2ts as legit content, no mpls wrapper). The right-editor's DuplicateGroupPanel gains five polish affordances mirroring the prototype (`research/MKV Auto UI/labeling.jsx:1653-1729`): size column (`{source · duration · size}`), **Make primary** button on non-primary siblings (reuses existing set-primary endpoint), new `DuplicateCompareModalComponent` opened from a **Compare** ghost button (side-by-side grid of every member with size/duration/chapters/audio/resolution + inline Make-primary), an **Ungroup** / **Re-group** toggle backed by new `disc_titles.force_independent_group` column (migration `202605230002`) + `POST /discs/{disc_id}/titles/{title_id}/ungroup-duplicate` endpoint for the false-positive escape hatch (two real playlists that legitimately share segments but differ in audio/subs), and human-readable **dup-reason subtext** per row ("Same segments as the primary", "Skipped by exploratory-rip match", "Component clip wrapped by this playlist", etc.). 31 backend duplicate_info tests + 9 helper tests + 38 frontend specs cover the matrix branches, sibling-into-wrapper absorption, force-independent grouping, compare-modal rendering + select/dismiss. Commits `77c0c0d`, `f6267a1`, `62860d8`, `780dee1`, `e380225`.
- **Selective rip + segment-reorder workstream** — End-to-end support for Midway-class playlist-obfuscation discs (Lions Gate 2019, Avatar UHD, etc.). When a disc has duplicate sorted-segment-map groups AND projected rip exceeds threshold (`MKVAUTO_RIP_REVIEW_THRESHOLD_GB`, default 200 GB), `POST /jobs/rip` returns 409 `needs_user_choice` and the frontend opens a threshold modal. User picks "Find canonical" → backend rips one exploratory playlist, generates per-PlayItem previews via fast+accurate ffmpeg seek, user drags previews into story order in a CDK-drag-drop UI, backend matches against on-disc playlists and re-rips the canonical via a per-title selective-rip loop that excludes the unmatched same-sorted-segment-map siblings. Default rip behavior is unchanged for normal discs. Phase 1: `Backend/core/bd_mpls.py`, `Backend/core/rip_selection.py`, scan-time `disc_titles.obfuscation_flag` capture from MakeMKV `MSG:3307` bit `0x01000000`, optional `rip_set` parameter on `core.disc.Disc.rip()`. Phase 2: `Backend/core/segment_reorder.py`, `Backend/core/path_a_trigger.py`, `Backend/core/path_b_dedupe.py`, three new endpoints (`/jobs/rip-with-segment-reorder`, `/jobs/{id}/segment-order`, `/jobs/{id}/segment-reorder/cancel`), workflow-context now exposes `dedupeGroups`. Frontend: `RipSizeWarningModalComponent`, `SegmentReorderPageComponent`, `DisagreementCompareComponent`, `ObfuscationBadgeComponent`. Migrations `202605030001` (obfuscation_flag + playitem_durations_s on disc_titles) and `202605040001` (segment_reorder_state + rip_set on jobs).
- **Auto-rip toggle (#331 partial)** — `auto_rip_enabled` setting persisted in `settings.json` (default false), with `GET/POST /system/auto-rip/config` endpoints. The on-scan-complete dispatch (DiscDB-hit auto-fire + DiscDB-miss rip-then-link) is a follow-up.
- **UI design system** — 14 primitives (`<ui-icon>`, `<ui-pill>`, `<ui-btn>`, `<ui-card>`, `<ui-field>`, `<ui-progress-ring>`, `<ui-stage-chip>`, `<ui-chip>`, `<ui-path-chip>`, `<ui-stepper>`, `<ui-empty-state>`, `<ui-section-header>`, `<ui-kbd>`) + 2 composites (`<ui-drive-card>`, `<ui-library-card>`) ship in `Frontend/src/app/ui/`. Foundation: 21 CSS tokens in `Frontend/src/app/ui/tokens/_tokens.scss`, JetBrains Mono added alongside Inter, Tailwind extended with token-derived colors. Devmode-only demo at `/preview-test/ui-kit` renders every primitive next to the prototype screenshots for visual diffing.
- **Screen re-skin against the design system** — Re-skinned the exploratory rip surface (rip-size-warning modal, segment-reorder page, disagreement-compare, obfuscation-badge, path-a-workspace), the ripper page (drive carousel, workflow breadcrumb pill-rail, workflow action bar), labeling (title-label desktop view restructured into list+editor split via new `TitleRowComponent` + `TitleEditorComponent`), the full Settings page (sidebar shell with 9 per-tab sections), the transfer-config list, History (Library) page chrome + boxset/release cards, the Setup wizard (chrome + all 6 step components, brand-color avatar tiles preserved), and the DiscDB Search page. Templates only — public contracts unchanged. History/Search/Settings page-level header chrome collapsed to a transparent inline title row matching the prototype's compact `Page` wrapper.
- **Devmode setup-page bypass** — `/setup?force=1` skips the redirect to `/ripper` when `ENABLE_DEVMODE=1`, so the wizard can be previewed without flipping the persisted `first_time_setup_complete` flag. Production builds get `enabled: false` from `/system/devmode`, so the bypass is a no-op. `scripts/mkv-lib/docker.sh` now forwards `ENABLE_DEVMODE` so `ENABLE_DEVMODE=1 mkv docker start` flips the backend on.
- **Frontend version polling — auto-reload in dev, toast in prod** — New `/system/frontend-version` endpoint returns a short SHA-256 prefix of the served `index.html`; the SPA polls it every 30s via a new `FrontendVersionService` (wired through `APP_INITIALIZER`). When the hash changes, dev tabs (`/system/devmode → enabled=true`) silently `window.location.reload()` so iterating in the docker container no longer needs a manual hard-refresh after every `mkv docker rebuild frontend`; prod tabs show a 12s "new version available" toast instead so in-progress label edits or rip confirmations aren't lost mid-flight. Reload is mediated by a `RELOAD_PAGE` injection token so the unit suite can substitute a spy (Karma's Chrome Headless forbids redefining `window.location.reload` directly). 6 service specs cover dev-reload, prod-toast, no-op-on-same-hash, reload-only-once, baseline-failure-skips-polling, and empty-version skip.
- **Tiered obfuscation detection (`obfuscation_reason` column)** — Replaces the binary `disc_titles.obfuscation_flag` with a tier-aware `obfuscation_reason` column (`Backend/api/models.py`, Alembic `202605170001`). Three tiers: **HIGH** `'segment_set_sibling'` (non-representative of a sorted-segment-set group, written by `Backend/core/path_b_dedupe.py:apply_obfuscation_reason_from_dedupe` from the workflow-context builder) + **HIGH** `'path_a_decoy'` (Path A skipped this row, written by `_mark_path_a_skipped_siblings_as_ignore`) render a red **Decoy** pill; **MEDIUM** `'makemkv_msg3307'` (MakeMKV's per-title bit alone, no group context) keeps the existing slate **Likely decoy** pill. The legacy boolean stays in sync so existing readers keep working. Fixes a known false-negative class on Midway-shaped discs (8 of 202 long playlists shared the canonical's sorted segment set but were not flagged by MakeMKV — the new `segment_set_sibling` rule catches them) and the over-projection concern on standalone short clips (MEDIUM visual is intentionally less authoritative than HIGH). `ObfuscationBadgeComponent` gained an `@Input() reason` that drives the tier; the inline "Likely decoy" pill in the sibling list of `TitleEditorComponent` swapped to the same component for consistency.
- **m2ts ⊆ mpls subsumption (`subsumed_by_title_id` column)** — An .m2ts file whose clip ID appears in some .mpls playlist's `segment_map` on the same disc is redundant data — the mpls already wraps it. New helpers `compute_mpls_clip_index` + `apply_subsumption_marks` in `Backend/core/path_b_dedupe.py` detect the relationship from the workflow-context build, mark the m2ts `type='ignore'` (hidden behind the existing Show-ignored toggle on the left rail), and stamp `disc_titles.subsumed_by_title_id` with the wrapping mpls's UUID. `TitleEditorComponent` surfaces the m2ts rows inside the wrapping mpls's right-panel DuplicateGroupPanel under a new **Component clips** section (`@Input() componentClips`), where the user can click any row to swap the editor onto it. Tiebreaker when a clip is referenced by multiple mpls: smallest `index` wins, deterministic across context reloads. Respects user-applied non-ignore types. Live-verified on Midway: 00451.mpls(2) now wraps 02807/02808/02809.m2ts cleanly.
- **Titles screen — Path A canonical badge + auto-ignored hidden by default** — The titles list now renders an emerald **User Selected** pill on the row whose `index` matches `segment_reorder_state.matched_playlist_index`, making it obvious which playlist Path A's segment-reorder workflow chose (e.g. 00539.mpls on Midway). The MakeMKV scan-time "Likely decoy" badge is suppressed on the same row — Path A's confirmation overrides MakeMKV's MSG:3307 obfuscation hint. Backend: new helper `_clear_path_a_canonical_obfuscation_flag` runs from both canonical_complete sites in `Backend/api/routers/jobs.py`. Frontend: new `matchedCanonicalIndex` input on `TitleLabelComponent`, threaded from `state.context.jobStatus.segment_reorder_state.matched_playlist_index` in the labeling component. Adopting the prototype's `labeling.jsx:1138` pattern, the left rail also hides `type='ignore'` content rows by default behind a **Show ignored (N)** ghost-button toggle above the list; Path A's `_mark_path_a_skipped_siblings_as_ignore` continues to mark all 200+ unripped Midway decoys as `'ignore'`, so they now collapse out of view (toggle on to surface them with a muted style).
- **Devmode — Re-lookup DiscDB action** — New `POST /discs/{disc_id}/relookup-discdb` endpoint bypasses the DB-side DiscDB cache and re-runs the TheDiscDB API call against a disc's existing content hash, overlays the returned track metadata onto existing `disc_titles` rows (matching by `source_file`), merges the discdb fields into `disc.disc_info`, and flips `jobs.discdb_result` on every job for the disc to `'hit'`. Internal `is_dev_mode()` gate (403 in production) so the write-once `discdb_result` invariant only relaxes for this dev affordance. Devmode menu surfaces it as a **Re-lookup DiscDB** button that toasts the outcome. Lets the user recover discs scanned during a simulated-miss session without `DELETE /discs/disc/{id}` + re-insert.
- **Devmode — "Disable DiscDB" toggle (renamed + default-flipped)** — `Backend/core/disc.py:load_db_info` and `Backend/core/disc_manager.py:query_discdb` previously raised "Dev mode: simulate DiscDB miss" unconditionally whenever `is_dev_mode()` was true. The two-label "Workflow Mode" toggle (DiscDB Hit / DiscDB Miss) is replaced by a single "**Disable DiscDB**" toggle that follows the devmode-toggle convention: default OFF = production behaviour (real lookups run), ON = simulate miss and force the miss workflow in the UI. Setting renamed `dev.workflow_mode_discdb_hit` → `dev.discdb_disabled` (with one-time legacy-read migration: old `hit=true` ↔ new `disabled=false`). Endpoints renamed `/system/workflow-mode` → `/system/discdb-disabled` (body `{"disabled": bool}`). Frontend SystemService methods renamed `getWorkflowModeDiscdbHit/setWorkflowModeDiscdbHit` → `getDiscdbDisabled/setDiscdbDisabled`. The production `MKVAUTO_DEVMODE_BEGIN/END` strip is unchanged.
- **Devmode menu polish** — `dev.quick_postprocess_tests_enabled` default flipped from `true` to `false` so fresh dev sessions don't silently keep stage backups and mock files around. The FFmpeg Detection toggle was renamed to **Disable FFmpeg Detection** with inverted visual binding: off (default) = detection runs normally, on = detection suppressed. Storage semantics unchanged; the label now reads as what the action does when you turn the toggle on.
- **Exploratory Rip workflow step (Path A breadcrumb integration)** — The selective-rip + segment-reorder flow now appears as a first-class **Exploratory Rip** pill in the breadcrumb between *Movie/Series* and *Release*, only on the miss flow when the active job has a `segment_reorder_state` (Path A trigger). All three Path A sub-phases (`exploratory_ripping` → `awaiting_segment_order` → `matching_playlists` → `canonical_ripping_pending`) live under this single breadcrumb step; `<app-path-a-workspace>` switches its inner cards as the stage advances. The workflow action bar's Continue activates when `segment_reorder_state.stage === 'canonical_complete'` and bumps `workflow_step` to `boxset` (miss) or `postprocess` (hit) so the workspace unmounts and standard labeling resumes. Navigating back to *Movie/Series* mid-rip preserves the rip; cancel-and-pick-manually remains a separate explicit affordance on the workspace cards. Backend mutations: `Backend/api/routers/jobs.py:start_rip_with_segment_reorder` (set), `_maybe_advance_canonical_complete` + `submit_segment_order` early-exit (advance), `cancel_segment_reorder` (clear). Frontend: new `WorkflowStep` value `'exploratory_rip'`, centralised step-order helper at `Frontend/src/app/services/workflow-step-order.util.ts` (replaces 13+ hardcoded step-array literals), `pathAActive$` now derives from `getEffectiveWorkflowStep` rather than raw stage. New doc at `docs/WORKFLOW_PATHS.md` captures the three workflow paths (Standard rip / Exploratory Rip / Path B dedupe disclosure), per-path step lists, and the future intent to combine Post Process + Transfer into a single breadcrumb pill.
- **Scan complete notification** — When a disc finishes scanning, emits action-required `scan_completed` (toast, notification history, optional Discord); payload supports opening Ripper on OS notification click. Legacy `notification_levels` migration now turns on action-required Discord when any error level was listed. **INFO** / **DEBUG** logs explain skipped delivery (channels off) and Redis dedupe; **WARNING** when scan completes without `mount_point`.
- **Desktop / OS notifications** — When the browser tab is in the background and notifications are allowed, backend-driven in-app notifications (same WebSocket stream and **in_app** preferences as toasts) also appear as native system notifications. The notifications dropdown offers a thin **Approve** / **Dismiss** strip to trigger the browser permission prompt (Dismiss hides it for the current tab session).
- **Notification preferences** — Settings → Notifications: choose in-app (WebSocket) and Discord delivery by type (informative, action required, errors); informative progress notifications are off by default; existing `discord.notification_levels` lists migrate into the new structure on load.
- **Disc ripping pipeline** — Insert disc, optional TheDiscDB lookup, manual labeling for unknowns, rip with MakeMKV, post-process (rename/organize), transfer (local, SMB).
- **First-time setup** — Wizard for MakeMKV key, transfer destination, preview settings, optional Discord webhook.
- **Duplicate title detection** — Grouping and comparison for duplicate titles with metadata tags and tooltips.
- **Notifications** — WebSocket toasts and optional Discord for rip/transfer events and failures.
- **Notification history** — Bell icon in header with dropdown panel; stores up to 100 notifications with read/unread and clear all.
- **Rip progress ring** — Determinate circular progress on Now Reading drive cards during rip.
- **Completed job badge** — Green check on drive cards when a completed job exists for the inserted disc.
- **Disc scan failure indicator** — Amber warning icon on drive cards when disc scan fails (MSG:5010 etc).
- **Progress sub-phase labels** — "Verifying…", "Validating…", "Finalizing…" shown when progress is at 100% but stage is not yet complete.
- **Mobile navbar** — Primary navigation (Ripper, History, Search, Settings) always visible as icons on mobile; external links moved to drawer.
- **Title disambiguation validation** — Label completion blocked when multiple titles share the same name without distinguishing fields (edition, season, episode, type).
- **Devmode detection test** — CI test that verifies all MKVAUTO_DEVMODE markers can be cleanly stripped.
- **Single-container Docker** — Image with frontend, API, Celery, PostgreSQL, Redis, root helper; optional embedded DBs.
- **Release repo and distillation** — Allowlist-based script to produce the public mkv-auto-release tree; push on version tag only.
- **Unraid** — Community Apps template and platform guide.

### Changed

- **E2E full-stack docs aligned with the post-#378 stack (closes #378)** — Refreshes ``docs/TESTING.md`` and ``Backend/tests/README_E2E_TESTING.md`` to describe the actual end-state after commit ``3346cca``: full-stack Playwright now runs against a **test Postgres on host port 5433** and a **test Redis on host port 6380** (not SQLite + Redis 6379, which was the original infra and the long-stale doc narrative). Adds runbook coverage of the four isolation defenses against a co-located production ``mkv-auto`` container — the ``e2e-full.js`` pre-flight guard, ``E2E_ALLOW_PROD_CONTAINER=1`` override, the ``broker_url="memory://"`` Celery hardening in ``e2e_bootstrap.py``, and the stage-callback intercept that replaces the old multi-uvicorn-worker plan. User-journeys table extended to list the three fixture-driven specs (``happy-path-miss``, ``happy-path-hit``, ``discdb-api-down``) and their ``E2E_FIXTURE=<profile>`` requirement. Env reference now includes ``E2E_ALLOW_PROD_CONTAINER``, ``E2E_REDIS_PORT``, ``E2E_PG_PORT``, ``E2E_DATABASE_URL``, ``E2E_FIXTURE``. Documentation-only — no code changes; the engineering for #378 shipped in 3346cca/4168720/fb561c9/e2b2567 across the May/June 2026 E2E series.
- **Postprocess→Transfer collapse — final docs rewrite (#325, #365 step 5d.4 — closes the transient/-drop arc)** — Surgical refresh of the three architecture docs to describe the end-state model. ``docs/POSTPROCESSING.md``: stale Phase-2-in-progress transition note replaced with a collapse-complete summary; overwrite/collision policy now documents the flag-aware rename destination (local → ``config.transfer_dir``, remote → ``transient/``); destination-structure section shows both branches; file-discovery + output-discovery descriptions point at ``resolve_postprocess_validation_root``; ``post_paths`` description clarified throughout to say "relative to the resolved rename destination" rather than "files in transient/"; destination-location section gained the flag-aware split. ``docs/JOB_CLEANUP.md``: "what gets cleaned up" table clarifies that ``transient/`` is typically empty for local jobs (remote-mode staging only); new "what is NOT cleaned up" subsection explains why library files at ``config.transfer_dir`` are never touched and points operators at ``/reset-postprocess`` / ``/restore-postprocess`` for partial-failed-job library cleanup via ``_clear_per_rip_postprocess_output``; helper descriptions note the no-op behaviour for local jobs. ``docs/JOB_STAGES_AND_PROGRESSION.md``: postprocess "What Happens" rewritten to call out both branches and the src==dest shortcut for local mode; ``file_path_stage`` discussion notes the cosmetic ``"postprocess"`` vs ``"transfer"`` quirk for the shortcut path (the two ``releases.py`` rename branches that gate on it are functionally identical, verified during the audit); two ``post_paths`` descriptions in the data-fields tables updated. **End-state model:** the transient/-drop is functionally and architecturally complete. Remaining ``post_state`` column drop and frontend ``WorkflowStep`` enum collapse are tracked as separate workstreams in ``docs/plans/postprocess-collapse-handoff.md``.
- **Postprocess→Transfer collapse — docstring + comment refresh for the post-5d.1 model (#325, #365 steps 5d.2 + 5d.3)** — Documentation-only cleanup PR. After auditing, decided to keep ``JobPaths.transient`` (not rename to ``staging``) — the name still describes a transient location and a rename would touch 126 sites across 26 files for marginal clarity gain. Instead, refreshed the docstrings + comments to reflect the new model: ``core/job_paths.py`` ``transient`` property now documents its two post-5d roles (remote-mode local staging for atomic uploads; safe fallback when no active local TransferConfig); ``core/transfer/protocols/smb.py`` ``src_path.name == "transient"`` special-case comment now explains *why* it's there (remote-mode rename writes to transient/ as local staging and SMB needs to strip that segment so contents land at the remote root rather than ``<remote>/transient/``); ``core/job_cleanup.py`` module docstring documents that the transient walk is a no-op for successful local jobs and notes that cleaning up partial library writes from a failed local-mode job is out of scope (the devmode revert endpoints handle that case via ``_clear_per_rip_postprocess_output``, shipped in #456). No code changes, no test changes. Remaining 5d work: **5d.4** — full rewrite of ``docs/POSTPROCESSING.md`` / ``docs/JOB_CLEANUP.md`` / ``docs/JOB_STAGES_AND_PROGRESSION.md`` to describe the end-state model.
- **Postprocess→Transfer collapse — drop `MKVAUTO_RENAME_DIRECT_TO_DEST` env-var conditional (#325, #365 step 5d.1)** — First PR of the 5d cleanup workstream. Removes the env var that gated the direct-to-destination flow; behavior is now determined solely by the active ``TransferConfig`` (local mode with a valid ``transfer_dir`` → direct-to-library; remote modes, no active config, empty ``transfer_dir``, or DB-lookup failures → ``paths.transient`` fallback). The env var was already a no-op once #457 flipped its default to ``"1"``; this PR removes the conditional + constants from ``core/transfer/path_resolution.py`` and the ``-e MKVAUTO_RENAME_DIRECT_TO_DEST=...`` forwarding from ``scripts/mkv-lib/docker.sh``. Test files (``test_resolve_rename_dest_root``, ``test_resolve_transfer_src_root``, ``test_path_resolution``, ``test_validate_postprocess_output_flag_aware``, ``test_devmode_revert_flag_aware``, ``test_startup_validation_flag_aware``) cleaned of env-var ``monkeypatch.setenv/delenv`` calls — coverage now exercises behavior via active-config presence/absence which matches the new model. The pre-#451 legacy-job error message in ``_try_src_equals_dest_shortcut`` previously said "Disable MKVAUTO_RENAME_DIRECT_TO_DEST for this legacy job" — updated to point operators at the actually-useful remediation (re-rip or ``mkvmerge -J`` backfill). 59 tests green across the 7 updated suites; no behavior change for any user. Remaining 5d work: drop ``JobPaths.transient`` / rename to ``staging``, remove the SMB protocol's ``"transient"`` special-case, remove vestigial cleanup walks, final docs rewrite (``POSTPROCESSING.md``, ``JOB_CLEANUP.md``, ``JOB_STAGES_AND_PROGRESSION.md``).
- **Postprocess→Transfer collapse — `transient/` drop default-on (#325, #365 step 5c)** — Flips `MKVAUTO_RENAME_DIRECT_TO_DEST` from default `"0"` to default `"1"`. For local-mode jobs the rename now writes directly to the active TransferConfig's `transfer_dir` and the transfer step src==dest-short-circuits via Segment UID lookup — no more per-job `transient/` staging copy. Operators on the pre-collapse layout can opt back by setting `MKVAUTO_RENAME_DIRECT_TO_DEST=0` in the container env; remote modes (rsync/smb/nfs) are unaffected (they always use local `transient/` for atomic-upload staging). Required a series of audit follow-ups after the 5b'b src==dest shortcut (#454) landed and live smoke testing surfaced adjacent code paths that weren't flag-aware: **#452** isolated stuck-postprocess from stuck-preview health-check dispatch (a job in the stuck-preview bucket with a freshly-started postprocess would hit the postprocess-reset action and corrupt the running job — fixed by gating the action on `stuck_postprocess_ids`); **#453** made the post-process validator (`validate_postprocess_output` + `validate_postprocess_preconditions`) flag-aware by extracting `_resolve_rename_dest_root` / `_resolve_transfer_src_root` from `workers/tasks.py` into a shared `core/transfer/path_resolution.py` (also added `resolve_postprocess_validation_root`, the third public caller of the same logic) and shipped a comprehensive audit doc at `docs/plans/transient-drop-flag-on-audit.md` classifying every `paths.transient` reference in the backend as FIXED / REFACTOR / FLAG-OK / DEFERRED / DEAD; **#455** made the startup `validating`-recovery in `api/main.py:_recover_inflight_jobs` flag-aware (was hardcoded to `paths.transient`, would fail any mid-postprocess restart under flag-on); **#456** made the devmode revert endpoints (`/reset-postprocess?clear_files=true` and `/restore-postprocess`) flag-aware via a new `_clear_per_rip_postprocess_output` helper that walks `job.post_paths` and unlinks only this rip's slots at the resolved destination — no bulk library walk. **Test posture across the 5c arc:** 19 new tests in #453 (`test_path_resolution.py` with three-way symmetry guards for rename + transfer + validation, `test_validate_postprocess_output_flag_aware.py` end-to-end), 3 new in #455 (`test_startup_validation_flag_aware.py`), 3 new in #452 (regression cases for both buckets), 6 new in #456 (`test_devmode_revert_flag_aware.py`), and 1 new in 5c (`test_new_default_is_flag_on` regression guard against a future PR accidentally flipping the default back). End-to-end smoke-tested on a real V for Vendetta 1080p rip: postprocess writes directly to `/data/mkvauto/library/Movies/V for Vendetta (2006)/`, transfer step completes in 1.48 seconds (vs. minutes for a real 18.9 GB copy) with `transfer_phase=verifying` skipping the `transferring` sub-phase, all 8 `disc_titles.segment_uid` values populated. Remaining work tracked in `docs/plans/postprocess-collapse-handoff.md`: step 5d removes the env-var conditional + `JobPaths.transient` + SMB special-case + cleanup walks + stale comments + final POSTPROCESSING.md / JOB_CLEANUP.md / JOB_STAGES rewrites. `post_state` column drop and frontend `WorkflowStep` enum collapse remain as separate workstreams.
- **Postprocess→Transfer collapse — transient/ directory drop (#325, #365 architectural cleanup, 12 PRs)** — Started the work to eliminate the per-job `jobs/<id>/transient/` staging directory and have `rename_outputs` write directly to the final transfer destination (or a transfer-protocol-appropriate pre-staging area). Lands as a gated migration: a new `MKVAUTO_RENAME_DIRECT_TO_DEST` opt-in env var routes local-mode jobs to `config.transfer_dir` instead of transient/, with safety fallbacks for remote modes (rsync/smb/nfs always use staging for atomic-upload semantics) and DB-query failures. Foundation: `Disc.rename_outputs` gains a `dest_root` parameter (#437) and the main caller passes it explicitly (#438); a `_resolve_rename_dest_root(job, paths, db)` helper at the prep call site (#439) gains the env-var + active-TransferConfig branch (#440). The bulk of the cleanup made the 5 `trans_root` walks in `_run_prep_phase` per-rip aware so an enabled flag doesn't make them over-count from a shared library: post-move verification walks only `renamed_paths` keys (#441), `files_already_moved` discovery prefers `job.post_paths` over a walk (#442), the `active_root` ternary uses `bool(renamed_paths)` instead of `any(trans_root.rglob)` (#443), the early `files_already_moved` detection (#444) and the resume validation count (#445) both adopt the same precedence chain. The same precedence pattern landed in the 3 `stage_validation.py` walks via a new `_per_rip_mkv_count_or_walk` helper (#446) so pre-flight + post-process validation are equally safe under the flag. Step 5b (#450) adds the symmetric companion `_resolve_transfer_src_root(job, paths, db)` so the transfer step reads from wherever rename wrote, wired at `Backend/api/routers/jobs.py:3752`; bg/celery transfer wrappers take `src` as a parameter so they're already correct downstream of this site. 16 new test assertions in `test_resolve_transfer_src_root.py` including 5 symmetry-regression guards that fail loud if the two helpers ever drift (drift would mean rename writes where transfer doesn't read, or vice versa — silent file loss). Wire-in audit surfaced that the handoff doc's assumption "existing copy logic detects src==dest and skips the copy" is not actually true — `use_final_map` would hit `SameFileError`, `transfer_local` would self-nest the destination; only the library_dirs branch accidentally works via its `if dst_file.exists(): continue` skip. **Sequencing revision** captured during #450 review: pull #448 (Matroska Segment UID capture, shipped as #451) forward as the next PR. With `DiscTitle.segment_uid` populated by `mkvmerge -J` at postprocess, step 5b'b's src==dest shortcut becomes a small UID-match join instead of a brittle `post_paths` filename walk, and the same primitive carries forward to #449 (self-healing reattach) for free. **Step 5b'b** (this PR) adds the `_try_src_equals_dest_shortcut` helper called from `transfer_job` immediately after `apply_job_state(transfer_state="running")` (jobs.py:~3933), before the scenario picker that would otherwise crash with `SameFileError` (use_final_map) or self-nest the destination (`transfer_local`). The helper returns False for remote modes / `src != dest`; when applicable it identifies this rip's files at the destination by Segment UID lookup, calls `_advance_transfer_phase(..., "verifying")` + `_complete_transfer`, and skips the copy entirely. Pre-#451 legacy jobs without captured UIDs, file-count mismatch, and UID-mismatch at the expected slot all fail loud with operator-actionable HTTP 500 messages rather than falling through to the broken copy paths. 11 new test assertions in `test_transfer_src_equals_dest_shortcut.py` — including a critical regression guard that mocks `shutil.copy2` with an `AssertionError` side-effect so any future refactor accidentally reverting the shortcut fails the test loudly. The flag is **now safe to opt into for local mode** (`MKVAUTO_RENAME_DIRECT_TO_DEST=1`); 5c (flip the default) waits for soak time to elapse so no in-flight job is pre-#451. 110 new test assertions across 9 new test files in this batch (`test_disc_rename_outputs_dest_root`, `test_resolve_rename_dest_root`, `test_post_move_verification_per_rip_walk`, `test_files_already_moved_per_rip_discovery`, `test_active_root_per_rip_selection`, `test_files_already_moved_detection`, `test_resume_validation_count_per_rip`, `test_stage_validation_per_rip_helper`, `test_resolve_transfer_src_root`), each including a regression scenario that documents the exact bug a step prevents (e.g. "100 unrelated library MKVs + 1 from this rip → per-rip count returns 1, pre-step rglob would have returned 101"). Remaining work captured in `docs/plans/postprocess-collapse-handoff.md`: 5c flips the default once 5b'b has soaked; 5d removes the env-var conditional + `JobPaths.transient` + SMB special-case + cleanup walks + stale comments + final POSTPROCESSING.md / JOB_CLEANUP.md / JOB_STAGES rewrites. Then post_state column drop and frontend `WorkflowStep` enum collapse remain as separate workstreams.
- **Postprocess→Transfer collapse — worker→API HTTP callback elimination (#325, #365 follow-up cleanup, 9 PRs)** — Followed Phase 2 (#425) with the full conversion of all six worker→API HTTP callbacks to in-process state updates, eliminating the #378-class fragility class entirely (worker no longer depends on a reachable localhost API to report state). Six conversion PRs (#427 postprocess-complete + body extract + recovery retarget; #429 transfer-complete; #430 transfer-progress; #431 rip-verification-complete; #432 rip-complete; #433 rip-progress) — each opens a worker-owned DB session, calls the in-API helper (`_complete_transfer` / `_fail_transfer` / `StageState.*`) directly via Python import, preserves all downstream side effects (WebSocket emit, milestone notifications, callback chains), and ships parallel test coverage with explicit `requests.post` monkeypatch regression guards that fail loud if a future refactor restores the HTTP fallback. The `resume_postprocess` Celery task body was extracted into a shared `_run_prep_phase` helper so both `start_transfer` and the legacy task name (kept registered for in-flight Redis tasks across deploys) share the prep logic in one place. Failure-raise semantics preserved across all conversions: handler exceptions are wrapped as `RipCallbackTransportError` / logged-and-swallowed identically to the prior HTTP transport-failure behaviour. Two endpoint-deletion PRs followed (#434 drop the 4 orphaned endpoints + their schemas + their HTTP test files; #435 drop the @router.post decorators from the remaining two whose handler functions are still imported in-process) — **net ~1,072 lines of code removed** between the two. 109 new test assertions across 7 new test files; all green throughout the series. The Phase 2 in-process transfer_phase sub-phase indicator (#428) added the helper that advances `transfer_phase` from "preparing" → "transferring" → "verifying" in both the sync and bg (rsync/smb/nfs) transfer paths, so the frontend's `transferPhaseLabel` ("Preparing files…" / "Transferring…" / "Verifying integrity…") progresses through all three sub-phases instead of being stuck at preparing. Helper is idempotent (no DB write on every progress callback) and exception-safe (state-write hiccup never crashes mid-transfer). Remaining work tracked in `docs/plans/postprocess-collapse-handoff.md`: drop `transient/` directory, drop `post_state` column, collapse frontend `WorkflowStep` enum, final docs rewrite.
- **Post-process** — `resume_postprocess` re-waits for raw MKV **quiescence** (when files are still under `raw/`), then **syncs `disc_titles.mkv_size`** from on-disk sizes before pre-flight (`db.flush` in-session to avoid SQLite lock issues with the completion callback). Pre-flight treats missing `mkv_size` on ripped titles as a **warning**; **output** validation no longer fails jobs on byte equality between transient outputs and `mkv_size`.

### Removed

- Obsolete CLI and temporary docs.

---

## [0.9.0] - 2026-02-01

### Internal milestone

Internal `1.0.0` development milestone — never shipped publicly. Relabelled to `0.9.0` when the actual public `1.0.0` was cut. First-shape of MKV-Auto: automated optical disc ripping and media management with disc detection, TheDiscDB lookup, manual labeling, MakeMKV ripping, post-processing, and multi-protocol transfer. Angular frontend, FastAPI backend, Celery workers, PostgreSQL, Redis. Single-container Docker image with optional embedded databases.

---

[Unreleased]: https://github.com/MKV-Auto/mkv-auto-release/compare/v1.0.0...HEAD
[1.0.0]: https://github.com/MKV-Auto/mkv-auto-release/releases/tag/v1.0.0

---

## Thanks

Bug reports and analysis that shaped a release. If you would rather not be
named here, say so on your issue and it will be removed.

- **@NoSadBeHappy** — the Ungroup button doing nothing in a duplicate group, and
  the support bundle that made it diagnosable ([#8], 1.6.2).
- **@Medformatik** — traced multiple-releases-per-series to the exact migration
  and the `IntegrityError` fallback, and separately found the disc-reassignment
  bug that came with the fix ([#9], 1.6.4).

[#8]: https://github.com/MKV-Auto/mkv-auto-release/issues/8
[#9]: https://github.com/MKV-Auto/mkv-auto-release/issues/9
